# Changelog

PastePanda 版本更新日志。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [Unreleased]

### 新增
- **按「事件」找东西**：顶栏新增事件筛选，把时间上连在一起的复制归成一阵（如「14:59-15:46 · Excel · 8 条」），选一阵就只看那阵的内容——适合「想起上周三下午那会儿弄的东西」
- **今日整理**（工具箱）：把一天的剪贴碎片按时间理成一条时间线——几点到几点在哪个应用、攻了多少条；可让 AI 写一段小结并存进今日速记。**全程只读时间与来源，不读也不发送任何内容**
- **库体检**：知识库顶部会提示库里有几项可以修——断链、只差大小写的重复标签与标题、几乎空白的笔记；点开能直接跳到出问题的那一篇。没问题时整条不出现

### 修复
- **来源名显示成了一串窗口标题**：来源筛选里大量条目显示为「模型对比分析 - DeepSeek…」这种截断文字，而不是应用名；同一个应用的不同窗口也各算一个来源。修后来源数从 341 个归并到 96 个，顺带修好了 Xterminal 被认成系统终端
- **接入外部 AI 工具时报错了标签**：知识库的 MCP 接口把剪贴板的自动标签（JSON、邮箱、代码…）也当成知识库标签报给模型，还据此发出一条不存在的「标签重复」警告；模型拿这些标签去筛只会得到空结果
- **自动标签的创建时间差 8 小时**：内置格式标签（JSON/YAML/…）写入时用了 UTC，而手动标签用本地时间，同一列混了两个时区

## [7.0.0] - 2026-09-02

### 新增
- **知识库（全新模块）**：顶栏新增知识库入口，把零散剪贴内容与长期笔记统一收纳，支持文件夹分类、空白笔记与版本快照
- **今日速记**：随手记下一闪而过的想法，入口已统一到顶栏
- **问答**：对知识库内容直接提问，回答流式呈现、标注引用来源 [n]、支持多轮追问
- **字段视图**：知识库与收件箱以结构化字段呈现，长列表不再被静默截断
- **Markdown 导入 / 导出**：整库目录结构可一键导出备份，也能再导入还原
- **转笔记模板**：把已有内容一键固化为可复用的笔记模板
- **笔记编辑器增强 + 轻量 AI**：自动生成一行摘要、建议标签
- **回收站**：笔记删除后进回收站，30 天自动清理，误删可随时恢复
- **知识库 MCP Server**：把知识库接入外部 AI 工具，提供只读查询、调用审计、客户端管理与 Host 头校验

### 改进
- **安装包品牌化**：NSIS 安装向导使用 PastePanda 图标与品牌位图
- **国内下载链路完善**：Gitee 下载与手册版本号自动注入，下载更顺
- **笔记预览不再被截断到 120px**

### 修复
- **安全**：自有凭证不再被剪贴板监听记录为明文历史
- **构建**：husky 测试门禁在沙箱环境下改用更稳的 worker 池

## [6.18.5] - 2026-08-26

### 新增
- **截图去水印新增「文字·自动」**：OCR 定位图中重复出现的水印文字（企业微信 / 钉钉 / 飞书那种整段重复的），一键批量还原；结果先预览、逐框可排除，整批一次撤销
- **去水印新增「魔棒」形状**：刷过水印文字即自动吸附同色连通区，斜向水印一笔选全，不用沿着字一点点涂
- **Markdown 全屏编辑器加大纲面板**：按标题层级导航长文档，点击跳转；纯编辑模式下也能用
- **Markdown 编辑器补齐字数统计**：状态栏显示字数与预计阅读时长，中文按字计、英文按词计
- **Markdown 编辑器补齐编辑器该有的快捷键**：Ctrl+F 查找、Ctrl+Z / Ctrl+Y 撤销重做、Ctrl+B 加粗、Ctrl+I 斜体
- **截图界面跟随 6 套主题**：工具栏、属性条、提示条、放大镜、长截图状态条等浮层全部接入主题；浅色主题下不再是一块深色玻璃压在浅色截图上

### 改进
- **长截图交互整体重做，改成微信那样**：框选 → 点「滚动截图」→ 自己滚鼠标滚轮 → 画面实时拼接 → 点「完成」。去掉了模式选择、自动滚动、「下一张」这些中间步骤
- **长截图带实时预览**：状态窗里直接看到已经拼了多长，配三色提示告诉你当前拼接质量（滚太快没重叠会明确提示往回滚一点）
- **长截图状态窗改贴在选区旁边**：不再跑到屏幕角落，眼睛不用来回找
- **Markdown 里粘贴的图片改存到文档旁边**：存进 `文档目录/assets/` 并写相对路径，文档整个目录拷走图片不丢；此前存在应用数据目录、写的是绝对路径，文档发给别人图全裂，而且这些图无法安全清理
- **Markdown 格式按钮支持多行选区**：选中多行一次性套用列表 / 引用 / 标题，有序列表自动递增编号，再点一次取消

### 修复
- **修复 Markdown 编辑器 Ctrl+S 保存的是打开时的旧内容**：编辑之后按 Ctrl+S 会把改动静默丢掉（工具栏保存按钮正常，只有快捷键这条路有问题）
- **修复外部打开的 Markdown 文件保存失败**：报「forbidden path」，只能另存为、不能原地保存
- **修复外部 Markdown 预览里本地图片不显示**：文档里 `./assets/x.png` 这类相对路径此前一律显示不出来
- **修复编辑区与预览区滚动同步越滚越偏**，以及某些情况下滚动同步会彻底失效
- **修复长截图后台守卫任务可能残留**
- **降低去水印的内存占用与处理耗时**

## [6.18.4] - 2026-08-23

### 新增
- **双击内容打开专属编辑器**：代码 / 命令、配置（.env、ini、key:value 表格化编辑）、日志（按级别过滤 + 关键字高亮）、邮箱与电话（校验 + 一键唤起邮件/拨号）、SVG（源码与预览双向编辑）、编解码、二维码，各有各的界面，不再一律扔进纯文本框
- **PDF 直接在应用里翻页阅读**：复制来的 PDF 不用先另存再打开
- **音频视频直接在应用里播放**：常见格式（mp4/webm/mp3/wav 等）内嵌播放，其余仍可用系统播放器打开
- **文本对比接入 AI**：对比结果可让 AI 解释差异、总结改动
- **截图和图片里的文字现在能被搜到**：识别出的文字进入全文检索，搜关键词就能找回那张图
- **完整功能手册上线网页版**：19 个分区图文详解，顶部按任务查（「把截图里的文字变成可编辑文本」这类），带页内搜索，可切换 6 套主题
- **更新弹框改版**：按「新增 / 改进 / 修复」分组的卡片式发行说明，支持分类筛选

### 改进
- **默认主题改为经典白**：首次安装打开即是亮色界面（原为深色）；6 套主题仍可随时切换
- **操作失败不再只写进日志**：15 处「点了没反应」的场景改为在界面上明确告知原因
- **更新弹框的「查看完整手册」直达手册正文**：不再落到宣传首屏、要滚过几屏才看到说明
- **SQL 与 SVG 内容会打开对应的专用编辑器**：此前只能当普通代码编辑
- **托盘弹窗自适应高度**：一屏显示完，不再出现滚动条

### 修复
- **修复搜索结果有时不刷新**：后端返回结果后列表仍显示旧内容
- **修复粘贴守卫的提示长期失效**：粘贴敏感内容时该有的确认提示没有出现
- **修复粘贴过的内容没被记为「常用」**：6 类粘贴入口漏记，导致这些内容可能被自动清理掉
- **修复部分内容搜不到**：文字索引在少数情况下未建立；没有拼音信息的记录也被跳过，拼音首字母搜不中
- **修复撤销有时不生效**，以及数据库临时文件异常增长占用磁盘
- **修复卡片右键粘贴图片 / 文件不生效**
- **修复截图拖选时画面闪动**，以及取色结果与实际颜色不一致
- **修复深色主题下橙色角标与按钮上的白字看不清**（栈序号、粘贴栈主按钮等）
- **修复「查看完整手册」打不开**：改用可正常访问的地址，并提供备用镜像

## [6.18.3] - 2026-08-21

### 新增
- **截图「去水印」**：平铺水印（企业微信 / 钉钉 / 飞书这类）一键自动去除，或手动涂抹局部；纯前端处理、可撤销、不联网
- **截图马赛克 / 模糊升级**：可选「涂抹 / 矩形」两种形状，强度档位各自记忆；模糊收进马赛克属性栏统一调
- **快捷键支持单键**：截图等热键可设为 F1-F24 / PrtSc 等功能键（Snipaste / PixPin 同款习惯）；字母 / 数字单键会被拒绝，避免劫持全局输入

### 改进
- **高亮真正"高亮"了**：改成半透明描线，无论选什么颜色，画过的内容始终清晰可见（不再把整行盖成色块、深色下直接看不见）
- **截图文字工具大改**：输入框看得清了、长行不再被裁；文字写到选区右边界才自动换行（微信同款），输入时和最终盖上去的字一模一样
- **马赛克 / 模糊盖完就固定了**：不会被二次点中拖走，隐私不再意外露出来
- **右键菜单重构**：结构拆分、键盘全程可操作（方向键导航、子菜单智能翻折），更稳更跟手
- **「正则替换」菜单实时刷新**：在规则库里增删、启停规则后，卡片右键菜单立刻反映，不用等

### 修复
- **修复截图窗一打开就显示「截图异常」**：截图必崩，一次都截不了
- **修复截图异常后按 Esc 退不出去**：异常面板自带 Esc 关窗，还能一键复制错误详情
- **修复截图窗白屏卡死没有任何提示**：窗口起不来时自动关掉并在主界面告知
- **修复点「完成」要等一两秒才响应**：文字识别前的图片编码改成异步，不再卡住界面
- **修复去水印对企业微信这类平铺水印不生效**：按「格点结构」识别，水印能明显变淡（受半透明水印原理限制，深色背景上仍难以完全去净）
- **修复并发保存同一张图偶发失败**：临时文件重名导致

## [6.18.2] - 2026-08-20

### 改进
- **AI 配置不会再"自己消失"**：配置还没加载完就改设置，会提示等它加载好再改，不再把默认值当成你的配置写进去、冲掉已配好的东西（此前真的发生过：已启用的内置免费被悄悄写成未启用）
- **「一键启用内置免费」现在真的立刻生效**：点完设置页、快捷区、AI 动作门控全部即时变成可用，不用再等 30 秒缓存（以前 toast 说"现在就能用了"，界面却还显示未启用）
- **AI 隐私提示更诚实**：只有本地模型（Ollama）才显示"内容不出这台电脑"；内置免费虽免密钥，内容仍发往远程服务商，不再误导
- **截图撤销更跟手**：只是点选一下元素、没真拖动，不再往撤销栈塞"按了没反应"的空步骤，Ctrl+Z 一按一个准
- **长截图取消更可靠**：预览态按 Esc 取消后松手，不会再误触发长截图

### 修复
- **修复"配好的 AI 自己没了"**：AI 高级设置折叠时会把旧值 / 默认值写回配置，已改为只在你改过并收起时才落盘
- **修复「开始配置」按钮点了没反应**：区块已自动展开时再点无反馈，现在会补上光标聚焦
- **修复截图橡皮擦在空白处擦会留下擦除轨迹**
- **修复截图「插入文档失败」无提示**：现在明确报错且不关窗，方便重试

## [6.18.1] - 2026-08-19

### 新增
- **设置页新增「截图窗口常驻」**：开启后截图窗关闭时仅隐藏、再次按热键秒开（微信同款）；关闭则每次冷启动、首次画面慢几秒。代价是常驻约几十~百 MB 内存
- **截图后自动动作链：没有链时直接给「创建」入口**：不再只看到一个孤零零的「不自动执行」，点「创建」直达动作链编辑器（顺带修好了编辑器新建入口点了没反应的问题）

### 改进
- **截图完成秒级响应**：点「完成」窗口立即关闭、图片立刻进剪贴板，复制 / 保存 / 识别在后台悄悄完成——以前大图要等一两秒甚至更久
- **截图选框不再"隐身"**：选区边框统一 3px 蓝色；全屏或贴屏幕边缘时边框自动向内收一点，不会再被屏幕边缘裁掉看不到（以前全屏截图时根本看不出选框在哪）
- **进入截图模式有明确反馈**：按热键瞬间屏幕变暗淡入；全屏框选时四边有暗带提示
- **设置页控件风格统一**：开关统一为滑块、选项统一为分段按钮——拆行格式 / 表头 / OCR 选字模式 / 自动框选当前窗口等不再各长各样
- **截图热键默认改为 Ctrl+Q**（2 键，避开 QQ / 微信的 Ctrl+Alt+A / Alt+A 占用；可在设置里改回）
- **新功能弹框显示完整卡片**：更新后的「新增功能」弹框能显示「有什么用 / 怎么用 / 配图」了（之前只显示成一条条文字）

### 修复
- **截图完成偶发「复制失败：剪贴板写入被拒」**：写入时自家监听器不再抢剪贴板，并加大重试覆盖，第三方剪贴板工具瞬时占用也能兜住
- **截图标注态画面被切成 4 段蒙版**：进入标注后残留的选区草稿导致蒙版错位（要拖一下边框才恢复）
- **窗口吸附矩形出界导致的选区错位**：后端返回的窗口 / 控件矩形统一钳制到屏幕内（DWM 阴影扩展 / UIA 超界控件不再产生出界矩形）
- **截图新功能弹框的功能卡片不显示**：CHANGELOG 数据格式与解析器不匹配，已两端统一（「有什么用」解析 + 渲染去星号）
- **长截图状态窗定位**：选区占满屏幕时状态窗兜底位置不再误报相交

## [6.18.0] - 2026-08-19

### 新增
- **自动打码认识更多隐私**：除了手机 / 身份证 / 银行卡 / 邮箱 / QQ / 微信号，现在还能自动盖掉 **IP 地址、车牌号、姓名（姓名：/ 收件人：/ 联系人：等标签后）、地址（地址：/ 收货地址：/ 户籍：等标签后）以及二维码 / 条码**
  为什么：截图里夹带的号码 / 地址 / 二维码常被忽略，一键把它们全盖掉，不用逐个手动画
  用法：标注工具栏点「自动打码」→ 识别结果先用橙色虚框预览，可点掉不想盖的 → 确认后整批打码，一次撤销全部
- **截图取文字不再藏在菜单里**：标注工具栏直接给一个「取文字」按钮，带识别中 / 完成 / 失败的状态角标，不用先完成截图再去找
  为什么：截图里看到字，不用先完成截图再翻菜单，点一下直接识别
  用法：点工具栏「取文字」→ 完成自动展开文字 → 逐行 / 逐字可选中复制（按 T 直接复制全文）
  配图：ocr
- **保存 / 贴图 / AI 从「⋯」里提到一级**：这三个是截图的主力出口，以前要先展开更多菜单才能用
- **马赛克 / 模糊 / 高亮可以“涂”了**：不再只能拖矩形，像笔一样抹过去就行；马赛克的格子对齐到全图同一套网格，来回抹多少下都是无缝的
  为什么：像笔一样抹过去就打码，不用反复拖矩形，来回抹无缝拼接
  用法：选马赛克 / 模糊 / 高亮工具 → 按住拖动涂抹 → 滚轮调强度（马赛克 / 模糊）
  配图：mosaic
- **真正的橡皮擦**：擦到笔迹会把它**切成多段**，而不是轻轻一划整条曲线全没（矩形 / 文字 / 序号仍是整个删，它们没有“一段”的概念）
  为什么：想精确擦掉一小段时，不会把整条曲线一起弄没
  用法：选橡皮擦工具 → 在要擦的笔迹上涂抹，被擦段立即消失
  配图：eraser
- **文字标注三档字号**，且字号按感知尺寸算，高 DPI 屏上不会越清字越小
- **高亮改用乘法混合**：叠三层也不会把底下的字糊死（荧光笔本来就是为了让那些字更醒目）

### 改进
- **工具栏重排：取消和完成放一起了**：标注工具栏的「取消」从中间挪到最右，与「完成」紧凑成对，误点靠颜色区分兜底（完成实心蓝 / 取消灰描边）；「更多 ⋯」并入保存 / 贴图 / AI 一组，出口更集中
- **截图底图改为无损 PNG**：旧实现是 JPEG q90，4:2:2 色度抽样把细字边缘涂花。顺带发现代码里“PNG 慢约 5 倍”那句注释是错的——在 2560×1440 上实测：PNG（Fast 压缩 + Up 滤波）**25.0ms**，JPEG q90 **150.3ms**。所以是既无损又快 125ms
- **窗口自动识别准很多**：排掉了 UWP 挂起壳窗与其它虚拟桌面的窗口（它们 `IsWindowVisible` 是 true 但看不见，Windows 自己的 Alt+Tab 也靠这个过滤）；命中测试与框出来的边界改成同一个来源，不再“鼠标在 A 窗上、框却跳到旁边 B 窗”；支持钻到控件级
- **遮罩笔刷加粗 8 倍**：线宽那 2/3/5px 是给矩形描边定的，当涂抹笔刷细到要来回抹十几遍才盖得住一行字
- **工具栏不再飘在屏幕正中**：跟着选区走，贴屏幕边缘时自动避让；选区尺寸标签同理（以前写死 `bottom: -30px`，选区贴屏幕底部时那 30px 落在屏幕外直接被裁掉）

### 修复
- **按钮悬浮提示不再被属性条挡住**：提示改为更高层级的浮层显示，且自动选择上弹 / 下弹方向——工具栏在选区上方时也会上弹、不会压住画布
- **截图时整个画面被放大**（非 100% 缩放的机器上必例）。截图窗口的尺寸与位置把**物理像素当成逻辑像素**传给了 Tauri，125% 缩放下窗口比屏幕大出 25%，底图被拉伸铺满 → 看到的画面就是放大 1.25 倍的。同一个原因还造成了下面两条：
- **窗口吸附框右侧 / 下侧对不齐**：坐标整体被拉伸，左上角不偏、越往右下偏得越多
- **屏幕右下区域吸附完全失效**：换算出的坐标超出真实屏幕范围，在屏幕外找不到任何窗口
- **贴图不是原尺寸**：两个问题叠在一起 —— 初始缩放写死 0.6 倍（小图还会被下限拉成不等比），且创建窗口时给的宽高是**外框**而图是按**客户区**画的，于是又少掉标题栏那一截。现在默认 1:1，只在图比屏幕工作区还大时等比缩到 90%
- **贴图只出一个空白框**：分层窗口在首次设分层属性前根本不被 DWM 合成。旧实现只在按 T（循环切透明度）时才调它，所以必须先盲按一下 T 才能看见图——而界面上没任何地方写着要按 T
- **打了码的文字仍能被一键复制出来**（安全）。OCR 行框在结果态是可点的，而它压在马赛克下面：点一下复制的是**整行原文**，遮罩等于没生效（表象是“点马赛克会冒个绿色框”，那是行框的「已复制 ✓」标签）。现在被马赛克 / 模糊盖住的行整行不可点，只要有一小段被盖就算（行尾的手机号也不能泄）
- **点“完成”保存到卡片后图片加载失败**：保存走 md5 去重，没加任何标注时结果图与 OCR 临时图像素完全相同 → 拿到同一个路径 → 清理临时图时把卡片的图一起删了
- **复制图片到剪贴板偶发失败**（os error 1418）：四个写剪贴板的入口统一走重试（递增退避 6 次）；失败时直接报出**是哪个进程正持有剪贴板**，不再只给一句“可能被占用”的猜测
- **文字工具看不到任何东西**：字号写死物理像素，18 物理 px 在 dpr=2.5 的屏上只有 7.2 CSS px；输入框无底色无描边，白字压白底图等于隐形；提交后文字还会往下跳一截（基线与输入框不一致）
- **模糊块四周一圈发虚透出原图**：卷积在临时画布边界处要采样边界**外**的像素，而那里什么都没有。现在从底图多取一圈边距再裁掉
- **底图未就绪时马赛克画成一片用户选色的实心格子**，看起来就像“这功能就长这样”；现在是中性灰占位棋盘，且底图加载失败会重试与明确提示
- **拖选时鼠标移出画布松手后残留半透明选框（幽灵框）**：现在移出画布松手也能正常收尾，不会卡在选区态
- **选区提交时未对齐磁吸**：预览（已磁吸）与落点（未磁吸）瞬间跳变，现在提交也走磁吸
- **重新选择后 hover 自动吸附失效**：重选后吸附恢复
- **反向拖出的选区点不中、无法编辑**：向右上 / 左下拉的选区现在也能点中
- **箭头只测杆不测箭头头部**：点箭头尖容易漏选，现在头部三角也能命中
- **窗口吸附 / 控件识别在部分机器上卡顿**：后端同步枚举窗口改为异步，不再饿死界面
- **贴图重复打开（HWND 复用）偶尔串图或打不开编辑**：修复贴图编辑的窗口复用竞态
- **截图后端健壮性**：BitBlt 失败不再静默黑图；COM 初始化配对释放（不再泄漏）；编辑器写入限定为 .md 白名单；120MB 限制文案修正（原误写 50MB）

## [6.17.2] - 2026-08-16

### 改进
- **OCR 识别引擎升级为 PP-OCRv6**：从 Windows 自带 OCR 换成纯本地 MNN 推理引擎（ocr-rs），中文与复杂排版识别率更高，且跨平台、模型随安装包发布（约 15MB）
- **OCR 构建完全离线**：OCR 依赖（ocr-rs 源码 + 预编译 MNN）vending 进仓库，构建不再联网下载；预编译库不入 git
- **工程整理**：项目迁移至新目录，Rust 编译产物清理；`claude.md` 移入仓库根作为开发规则

## [6.17.1] - 2026-08-14

### 新增
- **图片自动识别文字**：图片复制进历史后，卡片上自动显示识别出的文字，不用点开就能知道截图内容
- **一键复制识别文字**：悬停图片卡片点「复制文字」，或右键选「复制识别文字」，即可复制完整的识别结果
- **识别结果会记住**：同一张图只需识别一次，重启应用后仍然直接显示，不会重复识别

### 改进
- **图片识别更准确**：小图识别前自动放大，截图上的小字也能认出来
- **AI 栏显示识别文字**：处理图片时，AI 栏的摘要直接显示识别出的内容，不再是"图片 尺寸"占位
- **文档编辑器全新界面**：胶囊式类型徽章、纸面式内容区、带图标的分段标签、来源与时间信息、空内容时显示引导

### 修复
- 图片卡片标题不再显示"[图片] 尺寸"占位文本，改为显示识别文字（无文字时显示提示）
- AI 栏对图片的摘要不再显示占位文本

## [6.17.0] - 2026-08-14

### 改进
- **自动标签不再动辄「纯文本」**。拿本机 479 条真实历史（只算真正过分类器的那些）逐条回放：改动前 **77.9%** 的内容被标成「纯文本」。卡片上本来就显示类型，标签再说一遍「纯文本」等于什么都没说，看起来就像乱打的。现在降到 **53.7%**
- **新增「标识符」「编号」「文件名」三类标签**，把原先埋在「纯文本」里的 124 条内容认出来：`itemVO`、`SYSTEMCODE`、`C0805041350000005382`、`receivablebill_his.xml` 这些以前一律是「纯文本」。中文句子不会误入这三类（中文不用空格，“没有空白”对中文句子同样成立，这一点单独守住了）

### 修复
- **SQL 被三种不相干的判据分别抢走**：`set a='1'` 像配置行（3 条被标成 TOML）、`INSERT INTO t (A, B, C)` 每行逗号数相同像表格（1 条）、多行 SQL 又过不了代码检测的特征门槛（5 条直接漏成纯文本）。现在 SQL 有独立判据，先于配置 / 表格 / 代码
- **语言标签乱标**：`system` 里有 `em` 就算 CSS、`config` 里有 `fi` 就算 Shell、一个 `null` 就算 JavaScript——关键词是**子串**匹配。实测有一段 PowerShell 同时被标成 Shell 和 CSS、一段 Java 被标成 JavaScript。现在纯字母关键词要求词边界
- **中文段落被当成代码**：「模型把 16token 的额度全用在“思考”上了…」带引号、横线和英文 token，恰好凑够了代码特征比例。现在中文占比过半的内容不判代码
- **「claude.md 做任何功能都要考虑性能问题」被当成命令行**：`claude` 曾被收进常用命令表，而它同时也是日常句子的开头词
- **中英文的“超短内容”阈值差 3 倍**：判据用的是字节数，一个中文字占 3 字节，于是中文 5 个字就得去跑代码/命令检测，而英文要 15 个字符。现在统一按字符数

同一批 479 条上，可确认的误判从 **2.5% 降到 0%**。

## [6.16.0] - 2026-08-14

### 修复
- **AI 快捷条给短内容推的动作基本是错的**。拿本机 492 条真实历史逐条回放后发现：不到 10 字的条目里有 **79.7%** 拿到「润色纠错 / 提取要点 / 生成正则」这一模一样的三个按钮——给 3 个字的「配置组」提取要点，给字段名 `itemVO` 生成正则。原因是打分只判“这个动作能不能处理这类内容”，而“适用任何文本”对大多数动作都成立，于是六七个动作同分，最终由**注册顺序**决定你看到哪三个（同分情况占 52.6%）
- **类名、字段名、文件名、单据号被当成外文推「翻译」**，而且占了第一个按钮的 **31.1%**：`SYSTEMCODE`、`INCCForHHOrSSService`、`C0805041350000005382`、`receivablebill_his.xml` 全中。判据原来只看“是不是纯 ASCII”，而代码人的剪贴板里纯 ASCII 的绝大多数是标识符不是外文
- **「生成正则」「生成 SQL」「回复草稿」「生成周报」「合并整理」不再靠内容浮现**。它们的输入是“你想干什么”的描述或多段集合，内容本身给不出任何信号。实测占位：生成正则 44.9%、合并整理 17.3%、生成周报 15.0%。改后它们只在你打了对应标签、用过、或手动设为常用时出现
- **「继续处理」本不应出现在推荐里**（它是对上一次 AI 结果追问用的，没有“对哪段结果”就没意义）。代码注释一直这么写，但从未真正实现——它一直在参与打分，只是注册序靠后、被其它同分动作挤出前三名而没被发现
- **「一句话摘要」「改写语气」几乎从不出现**（实测 2.0% / 2.8%），因为只有它们老实守着 200 字 / 50 字的长度门槛，而没门槛的动作反倒占满了位置。现在所有动作在同一套长度规则下

### 改进
- 短文本兜底不再给“根本不适用”的内容硬塞动作。附带发现：这段兜底此前是**死代码**，492 条一次都没触发过——因为“适用任何文本”的那两个动作给每条内容都垫了底分，“一个候选都没有”永远不成立
- **代价说明**：按本机历史回放，约一半的复制内容（很短的标签、标识符、单号、纯数字）**不再显示 AI 栏**。这是有意的：那些内容没有任何 AI 动作能帮上忙，摆三个点了也没用的按钮比不摆更差

## [6.15.0] - 2026-08-14

### 修复
- **开机一小时内启动时，保存设置会让配置功能整体失效**（开机自启动的人几乎必然踩到）。原因是备份限频的初始值拿“当前时间减一小时”算，而系统计时器从开机才开始走——开机不足一小时时这个减法会直接崩，而它在一个全局初始化里，崩一次就会连带后续所有读写配置的地方
- **图片 / 图文 / 文件 / 从语义搜索结果粘贴时，之前不会记下“这条被用过”**。后果不只是统计少几条：历史记录的“按价值豁免过期清理”靠这个信号判定一条内容有没有被用过——**意思是你天天粘贴的图片，在清理逻辑看来等于从未被用过，到期会被当成无价值内容清掉**。四条路径已全部补齐

### 改进
- 粘贴时多记两个本机统计字段（粘的是列表第几条 / 往哪类应用粘），用于判定“按目标应用重排列表”这个功能到底值不值得做——先量再做，而不是先做再猜。只存一个下标与一个类别枚举，**不含任何内容本身，不出本机**

## [6.14.0] - 2026-08-14

### 新增
- **变换可以设为「常用」，永远排在最前**：卡片上点☆就行，变换中心会多出一个「常用」分组排在「推荐」之前。以前只能说“别推这个”（不再推荐），没有任何办法说“这个给我排前面”，只能等系统自己学
- 置顶在首页的 AI 快捷条里同步生效（那里只显示三个，所以置顶直接决定你看到哪三个）

### 改进
- 置顶的变换**不再受排序分数门槛限制**。很多真正常用的工具分数并不高（分数算的是“能不能处理这类内容”，不是“你会不会想用”）——不绕过门槛的话会出现“明明置顶了却看不到它”
- 设为常用时自动取消该变换的「不再推荐」，不会出现“在常用组里却又被标为不推荐”的矛盾状态
- 「清空全部学习记录」**不会删掉常用置顶**——那是你手动设的偏好，不是系统学来的（与输出偏好指令一致）

## [6.13.0] - 2026-08-14

### 新增
- **画像开始真正影响 AI 输出**（设置 › AI › 自进化，**默认开**）：把你的使用习惯压成一段不超 300 字的描述，随每次 AI 调用拼进指令里——常处理代码就叫它“优先解释思路而非逐句翻译”，常用 JSON/SQL 就叫它“保持结构与字段名不变”。在此之前，四层记忆里只有画像（语义层）从未改变过 AI 的行为，只在设置页展示
- **设置页把注入的那段文字原样展示出来**（等宽字体、可选中复制）。展示的就是实际发出去的那串字符，不是另外生成的描述；“开了却什么都没发”的三种情况（使用记录不足 30 次 / 习惯倾向不够明显 / 含疑似敏感信息被拦）都会直接写出原因

### 改进
- 注入片段与剪贴板正文同过一道出网闸，且它只由本地固定文案组合而成——不含任何复制内容、不含自定义动作名、不含任何你手写的字符
- 你手写的「输出偏好指令」拼在画像描述**之后**：两者冲突时以你显式写的为准，不会被统计推断出来的句子压过去
- 习惯描述变了会自动绕开 24 小时结果缓存（否则早上的旧结果会被原样吐回来，注入形同虚设）；关掉开关时零开销，旧缓存照常命中
- “当前处于你的高频时段”这句只在**当前时刻真落在高峰区间内**时才发，不会出现“高峰在上午、晚上十点却告诉模型现在是高峰”这种与事实相反的前提

## [6.12.0] - 2026-08-14

### 新增
- **流程图 UI 分两套壳按画布实际宽度切**（不是按弹窗 / 全屏切）：宽档（≥760px）左侧常驻形状竖栏 + 顶栏 + 右侧属性面板；紧凑档改成工具栏 popover + 贴底抽屉。**按窗口宽判是错的**——窗口 800px 时弹窗画布只有 696、全屏却有 768，同一窗口尺寸下两个壳该归不同档
- **节点属性浮岛**：选中节点后贴着选区弹一排高频动作（4 种形状 / 3 个颜色 / AI 展开 / 删除 / 「⋯」展开全部）。浮岛的门槛（380px）与三栏布局的门槛（760px）**是两个无关阈值**，当初把它们当成一个开关，导致 600px 窗口下浮岛永远不出现
- **右键菜单**（节点 / 连线 / 画布三种上下文），两档共用一份
- **形状可从形状库拖到画布落点**，拖拽时显示落点预览；点一下加到画布中心的旧行为保留
- **连线线型**：实线 / 虚线 / 粗线，对齐 Mermaid 的 `-->` / `-.->` / `==>`，**往返不再丢**（解析器一直认得三种，但以前没把「是哪种」存下来，导出一律写回 `-->`）
- **区域框（subgraph 分组）**：可拖可缩放的框，标题栏拖动时带走框内节点。归属**按几何包含算**（节点中心落在框里即属于该框，重叠取面积最小的框），不存成员字段——拖出框即脱组，不会留下指向旧框的残留。不用 React Flow 原生的 `parentId` 容器，是因为它会把子节点 `position` 变成相对坐标，而 `pickEdgeHandles()` 直接拿 `position` 比大小判上下行，父子混算会让所有回边的锚点判定作废
- **Mermaid `subgraph` 双向往返**：以前 `subgraph` / `end` 被 `DIRECTIVE_RE` 整行丢弃，导入一个带分组的图分组就静静没了；现在按组栈解析成区域框，导出时按几何包含反推、支持嵌套
- **节点缩放**：选中后拖八个手柄改宽高。同时把 `autoLayout` / `autoLayoutElk` / `pickEdgeHandles` 里写死的 `168×64` 改成读节点实际尺寸——不改的话，拉宽到 300px 的节点在自动布局里仍按 168 排，相邻节点会压上去
- **节点文字色**（含「默认 = 跟随主题」一格）
- **形状库按语义分组**：基础 / 流程 / 数据 / 分组
- 自动布局在有区域框时走 dagre 的**复合图**（cluster），保证同组节点排在一块儿并直接拿到框的新矩形
- **粘贴栈支持表格自动拆行入栈**：栈模式下复制表格会自动按行拆开分别入栈，依次粘贴一次贴一行；也支持竖着复制的一列数据（无表头）自动按行拆分；可在设置里选拆分格式（原始行 / 字段:值）与是否保留表头，误拆时“⋯”菜单一键撤销
- **粘贴栈新增“存为模板”与“模板库”**：把当前队列存成模板，下次直接从库里一键载入复用
- 合并粘贴新增两种格式：Markdown 列表、按段落智能换行（短句紧凑排列，长段落之间空行隔开）

### 改进
- 粘贴栈队列条目支持鼠标悬浮查看完整内容（自定义悬浮卡片，不再依赖系统原生提示，速度更快、不会被其他内容遮挡）
- 粘贴栈拖拽调整顺序更可靠（队列内容变化时也不会拖错条目）
- 粘贴栈的“移除条目”“撤销拆分”等操作补上了操作反馈提示；连续“全部粘贴”时不再每条都弹提示刷屏
- “粘贴+Tab”推进更可靠：改用真实按键注入，避免长按热键不放时误触发系统的任务切换

### 修复
- Markdown 全屏编辑器从外部 .md 文件打开时，双栏预览的滚动不会跟编辑区同步
- “文档”自动标签一直没真正生效过（种子表里漏注了这个标签名，写入时被静默跳过），补上后流程图也新增了自己的自动标签
- 流程图卡片的来源标识之前被写死成“流程图”（本该是“从哪个应用来的”），与微信/Chrome 等真实来源并排时语义错位，现改为应用名、类型标识改用自动标签
- 顶部“文本”筛选与批量清理之前不包含流程图/文档，导致只在“全部”里才能看到、误以为丢了；现已对齐
- **导出全部失败**（`fs.write file not allowed`）：Tauri v2 的 `fs:default` 只有读权限。补 `fs:allow-write-file` / `fs:allow-write-text-file` 两条**命令权限**即可，**不需要开 `**` 的 scope**——`tauri-plugin-dialog` 在用户选定路径后会自己 `s.allow_file(&path)`，前端仍然只能写用户刚选中的那个文件
- **全屏时导出下拉被形状栏挡住**：`.toolbar` 的 `position:relative + z-index:1` 造出一个层叠上下文，把 `.exportMenu`(z-20) 关在层级 1 里，而 `.shapeLibrary`(z-6) 直接参与外层上下文。点导出项实际点到的是形状库，会凭空多一个节点
- **打开编辑弹窗时按 Delete 会删掉主窗口选中的卡片**：`App.tsx` 的 `dialogOpen` 只写了几个 `show*` 局部变量，九个 `dialogStore` 弹窗一个没算（而同一处的 Esc 分支却检查了 `d.editorItem`，两者从来没同步过）。判断挪进 `dialogStore.ts` 的 `anyDialogOpen()` 做单一数据源
- **AI 展开子流程的节点全叠在一起**：落点是 `Math.random()` 撒在一个 160×60 的框里，且随机数让撤销 / 重做不可复现。改成对子图跑一次 `autoLayout` 再整体平移
- **属性浮岛测量不到尺寸就永久隐藏**：首帧 `anchor` 为 null → 组件提前 return → 没有 DOM → `useLayoutEffect` 的依赖之后再不变化，`visibility: hidden` 就再也解不开。改用 `ResizeObserver`
- **弹窗比全屏露出更多属性**：抽屉开关把「量不到尺寸」的失败态当成了「画布极窄」而自动展开全属性——弹窗有入场动画、测量比全屏晚，正好落这个分支
- **箭头大小随线宽变**：`markerUnits` 默认是 `strokeWidth`，选中时线加粗，箭头跟着抖；改成 `userSpaceOnUse`
- 二维码 / 卡片列表 / 画像导出的异常被吞成泛泛的「保存失败」，统一用 `errText` 透出真实原因

### 清理
- `DiagramToolbar.tsx` / `DiagramPropPanel.tsx` 拆成两套壳（`RoomyChrome` / `CompactChrome`）+ 共用叶子控件，`DiagramCanvas.tsx` 从 445 行降到 298 行（规则 #7）
- 删掉 `styles.minimap` 这句死代码：CSS 里从没声明过 `.minimap`，取到的是 `undefined`

## [6.11.0] - 2026-08-13

### 新增
- **流程图编辑器**（新内容类型 `diagram`，内嵌弹窗 + 全屏两种形态共用一套画布）：
  - **11 种节点形状**，取值集**故意以 Mermaid 的形状词汇为界**——导入 / 导出 / AI 生成三条路全走 Mermaid，加一个 Mermaid 表达不了的形状一导出就丢、往返就破。菱形 / 六边形 / 平行四边形 / 梯形用 `clip-path` 配一对伴随伪元素画（**裁伪元素而不是裁 `.node`**：`clip-path` 会连子元素的命中区域一起裁，裁盒子会把四个连接点切掉外半边）
  - **Mermaid 双向闭环**：从 Mermaid 文本导入，导出 Mermaid / PNG / SVG / `.panda`
  - **两套自动布局引擎**：dagre（紧凑）/ elkjs（大图）/ 自动（按节点数选）
  - **连线锚点按相对位置算**：同列走上→下、同行走左→右、回边走同侧绕行；手拖过的边不被重算
  - **焦点路径**：标记焦点节点后，与它相连的连线走流动虚线
  - 撤销 / 重做、框选、节点颜色 / 描边 / 字号、双击改文字
- **流程图专用 AI 动作三个**：`ai-diagram`（根据描述生成）/ `ai-diagram-expand`（展开子流程）/ `ai-diagram-label`（润色文案）。**不复用 `ai-rewrite`**：它的服务端模板是「用…语气改写下面的内容」，把指令塞进待改写位置会让模型改写指令本身。这三个是**画布内部动作**，已从 `ai_list_actions` 过滤，不会被 `initAiTransforms` 注册成卡片变换
- **Markdown 渲染器支持 mermaid 代码块**直接出图

### 修复
- **`parseMermaid` 整行丢弃同行「声明 + 连线」**：两条整行锚定的正则只能匹配“纯声明行”或“纯连线行”，而 `A[开始] --> B[结束]` 两边都不是——改成逐 token 扫描
- **`shapeAndLabel` 除了无引号 `[…]` 之外全部解错**：改成成对的 open/close 表，并修好 `toMermaid` 里 ellipse 与 pill 的包裹符碰撞
- **所有连线从节点顶部接到顶部**：导入 / AI 生成的裸边没带锚点，React Flow 会回落 `handles[0]`——也就是声明顺序里的第一个（Top）
- **菱形文字被转成 -45°**：旧实现从设计稿抄了一个反向旋转，但容器本身没转（只有 `::before` 转了）
- **空流程图会与历史条目 md5 合并**：`insert_diagram_history` 对“零节点文档”跳过去重，否则新建的空图会套到上一个空图的记录上
- **`.shape_rect` 未声明**：组件拼的是 `shape_` 前缀拼形状名，缺了就取到 `undefined`，className 里出现字面量 `"undefined"`；同时加 `asShape()` 在数据层收敛未知形状值

### 清理
- 移除未使用的 `zundo` 依赖（撤销栈是 `useDiagramModel` 里手写的，全项目零引用）

## [6.10.0] - 2026-08-12

### 新增
- **AI 栏建议按内容与标签细化**：
  - **打分收口到变换中心**：AI 快捷栏不再自己判内容。旧实现里 18 种 `content_type` 只判了 `link`、后端 16 个 AI 动作只硬写了 5 个，于是**代码会被推「翻译」**（代码里拉丁 token 占比必然过线）；现在消费 `applicableTransforms()` 的排名，code/json/config 推的是解释代码 / 修错 / 生成 commit
  - **语言级标签进 prompt**：`ai-explain-code` / `ai-fix-code` 会写明是哪门语言（Rust / Java / SQL 等 10 种，由后端 `ContentClassifier` 识别，前端不参与）——`content_type` 到 `code` 就到顶了，这一层才分得出是哪门语言
  - **`ai-json-to-type` 新增「目标语言」选项**：TypeScript / Java / Rust / Go / Python（默认 ts，与改动前行为一致）。它的目标语言拿不到语言标签（输入是 JSON），只能由用户选
  - **标签作为意图上下文**（新设置项 `ai_tags_as_context`，**默认开**）：手工标签名随内容发给服务商，激活「回复草稿」「周报」这类靠**意图**而非文本特征的动作（“这条是要回复的”文本里判不出来）。代价已在设置里写明；**标签名与正文同过一道出网闸**（原先只扫正文，一个叫“张三 138…”的标签会直接绕过）；只传 manual 标签，auto 标签后端自己就有
  - **图片/文件条目也能出 AI 栏**：它们 `text` 为空，以前整条不渲染；现从 `content` 的路径派生输入，命中现有的文件名 / 斜杠互转（本地零成本）。**路径派生的输入禁用全部出网动作**——路径含用户名与目录结构，不能因为“反正有个输入”就隐式发给模型
- **主窗口顶栏 AI 标识**（品牌蓝→紫渐变字），并把全站六处各长各样的 AI 标识统一成 `AiMark` 组件
- **AI 栏目标摘要行**：告诉用户“现在处理的是哪一条”（跟随焦点，焦点为空回落最新条；目标不在当前筛选结果里时明确标出）
- **序列转移表接入推荐排序**：“上一个动作 → 下一个动作”的条件概率参与打分

### 修复
- **AI 栏来源图标不跟设置**：`source_icon_mode` 默认值就是“真实程序图标”，而 AI 栏只调 `resolveSource()` 拿预设 emoji，于是默认配置下卡片/侧边栏/顶栏显示真实 exe 图标、只有 AI 栏显示 emoji
- **AI 栏摘要行挤在同一行**：`.bar` 缺 `flex-wrap: wrap`，而摘要行靠 `width:100%` 换行——不换行时它只是个“想占满整宽但可被压”的同行子项，五个子项互相挤压
- **变换中心动作链入口横向溢出**（~406px 内容塞进 408px 盒子，三个独立成因）、**动作链运行器垂向溢出**（整个弹窗没任何滚动容器）
- 快速回到顶部按钮位置（`bottom: 80px` 是个魔术数字，其语义与 `.cardList` 的底部内边距对不上）
- **AI 动作一直没记行为埋点**：AI 快捷栏是 AI 动作的主入口却不记事件，导致它们在推荐排序里的权重恒为缺失

### 内部
- **三处重复实现收口**（规则 #11）：
  - 来源图标双模式 → `useSourceIcon`（原先 SourceBadge / Sidebar / TopBar / GeneralTab 各一份，**AI 栏抄漏了一份**，正是上面那个 bug）
  - `parseFilePaths` → `lib/utils.ts`（原先三份且**行为不一致**：一份把数字/null 当路径，一份 JSON 解析失败时不按换行切）
  - 敏感内容判定：`aiQuick` 不再自己调 `maskSensitiveText`（`maskTransform.detect` 在判同一件事）
- **`AiQuickBar.tsx` 666 行拆成 6 个文件**（规则 #7：单组件 ≤300 行）：`hooks/useAiQuickRun`（执行编排 + 代际守卫）/ `ai/AiQuickResult` / `ai/AiQuickActions` / `ai/FollowupInput`（与 TransformCard 共用，再寄在对方里会成环依赖）/ `ai/quickTypes`，主组件 236 行
- `build_prompt` 第四参数改为 `PromptCtx` 结构体（带 `Default`）：语言与用户标签是连着加的两项，分两次改签名等于把二十处测试调用改两遍
- 新增 `ContentClassifier::language_from_labels()`；标签提权用 `Math.max` 叠在基础分上而不是塞进 `HAND_TUNED`——后者会让 `ai-reply-draft` 等动作在无标签时丢掉现有通用分、直接从变换中心消失
- 品牌渐变色逐主题验过 4.5:1（渐变字两个端点都得单独达标），并补上 `@media (forced-colors: active)` 回退——高对比度模式会换掉 background，`color:transparent` 的渐变字会彻底消失
- Rust 单测 +5（语言进 prompt / 无语言时句子干净 / target 选项 / 标签注入位置 / 空白标签不改 prompt），前端单测 +12

## [6.9.0] - 2026-08-11

### 新增
- **签到送 Token 免费额度（内置免费模型 Agnes，用户免配 key 打开即用）**：
  - **内置免费服务商 builtin-agnes**（第 17 家）：免 key + 公共免费 key，模型 agnes-2.5-flash，128K 上下文
  - **初始 10 万 token**，打开即送；**每日签到**：第 1 天 2 万 → 每天 +1 万 → 第 4 天起封顶 5 万/天，连续签到 streak（初始 + 签到累计 ≤ 100 万）
  - **兑换码**：离线验证（本地签名），**不设上限**（仅防数值溢出）；生成脚本供运营批量发码
  - **本地限流 ≤10 次/分钟** + 每日用量上限 10 万——保护免费 key 的 RPM（合规护栏）
  - **入口三件套**：① 左下角每日签到浮窗（WorkBuddy 式，每天启动弹出、非 modal、1.8s 自动收起）② 完整签到弹窗（余额环形 + 7 天徽章阶梯 + 兑换码）③ 设置 → AI 页只读额度卡
  - 主窗口「余额不足」警示胶囊（异常态，非常驻）；AI 动作触发时余额不足提示条引导签到/兑换
  - **与自配服务商完全隔离**：免费额度只在内置服务商生效，切到自己配的服务商按金额计费，两条账本互不触碰（见规划文档 §10.11）

### 内部
- 新增 `data_store/quota.rs`（账本单行表 ai_quota）+ `commands/quota.rs`（ai_quota_get/sign/redeem）+ 兑换码引擎（md5 类 HMAC，免新依赖）
- `ai_run` 内置服务商分支：配额检查 → 限流 → 调用 → 成功扣减（缓存命中不计费）；`is_local()` 改为显式白名单（ollama），防止内置免费被误判成本地跳过出网闸
- Rust 单测 +7（初始化/签到阶梯/封顶/兑换幂等/坏码过期/扣减每日上限/码往返），前端单测 +6（奖励阶梯/7 格阶梯/周累计）
- ⚠️ **发布前需替换** `provider.rs` 的 `BUILTIN_AGNES_KEY` 为真实免费 key

### 额度账本的两条后补修复
- **兑换码不再被 1 亿“安全上限”夹取**：原实现 `amount.min(SAFETY_CAP - granted)`，`granted` 撞到 1 亿后到账额变 0，却照样把码写进 `redeemed` 并返回 `ok:true`——用户看到“兑换成功”、额度没变，而这张码永久作废且本地记录说“已使用过”。兑换**本来就不受额度上限**（上限只约束签到），现改为只用 `saturating_add` 防 u64 溢出，不做任何夹取
- **签到与兑换改为单锁原子**：两者原本走 `quota_row()` / `save_quota()`（各自加锁再释放），“读完释锁、写时重新加锁”留有丢更新窗口：并发双击可能让同一张码兑两份、同一天签到领两次。现与 `quota_spend` 一致，全程持同一把连接锁；并删除自带加锁的 `save_quota()`，**所有写入只留 `save_quota_locked` 一个口**
- Rust 单测 +2（兑换累计越过 1 亿仍足额到账；并发兑换总额精确且不死锁）

## [6.8.0] - 2026-08-11

### 新增
- **用户粘性体系（P1 数据见证 + P2 AI 人格化）——让"你的数据"被看见**：
  - **我的轨迹（A2 活跃日历 + 连续周数）**：画像页新增 12 周活跃贡献日历（GitHub 式，颜色深浅 = 当天操作量）+「连续活跃 N 周」火焰徽标——你的使用痕迹第一次看得见摸得着
  - **成就墙（A3 真实成就导向）**：画像页新增 8 枚成就徽章（第一次飞跃 / 结构化人生 / 我会排错 / 方法论之父 / 可移植的灵魂 / 百炼成钢 / 老兵不死 / 画像觉醒）——每个成就都对应一个深度功能或长期积累，不奖励"打开次数"这类虚荣指标；未解锁显示引导提示
  - **效率见证（B3）**：剪贴板周报概览从 3 格扩到 5 格——新增「≈N 分 AI 省时」（7 天 AI 调用 × 单次估算，诚实标注"约"）与「连续活跃 N 周」
  - **本周挑战（B2 画像驱动）**：周报新增挑战卡——基于行为缺口推荐"下一个值得试的能力"（没用过 AI → 推荐 AI；没建过链 → 推荐建链），完成零惩罚、与成就墙联动
  - **里程碑时刻（B1 "有思想"的 AI 祝贺）**：三个画像驱动的关键时刻弹窗——安装周年（"你用 PastePanda 1 年了"）、第 10 万次复制、画像首次 AI 精炼（"我开始读懂你了"）；文案全部由本地统计驱动，只弹一次、可关闭
- 画像导出 / SKILL 安装成功后记入行为事件（成就「可移植的灵魂」判定源）

### 内部
- 新增 `stats_sticky` 命令（data_store/sticky.rs）：84 天活跃日历 + 连续周数 + 成就/里程碑原料一次取回，纯本地只读聚合，**不含任何内容**
- 成就/挑战/里程碑判定全部在前端本地完成（lib/achievements.ts / challenges.ts / milestones.ts），已读状态存 localStorage
- 数据层测试 +5（日历/周数中断/成就里程碑/事件兜底），前端单测 +23（成就 11 / 挑战 5 / 里程碑 7）

## [6.7.0] - 2026-08-10

### 新增
- **会话级编排（v6.6）——复制内容自己会"排错"了**：
  - 预置链 **`error-triage`（排错流水线）**：「AI 合并」→「AI 修复代码」两步编排，第二步带 `is-code` 条件——只有合并结果真是代码才触发修复
  - 会话感知建议升级：整桶都是报错代码（ERR_RE 匹配）时，建议条变成**「一起排错」**并预选排错流水线；普通代码会话仍建议「AI 合并」
  - 链结果含脱敏内容时显示绿色提示条（ShieldCheck 图标）——跑完让你知道"敏感保护已生效"
- **生成 SQL 动作（ai-sql-generate，v6.7）**：粘贴文本 / CSV → 生成 SQLite 兼容建表 + 插入 SQL（中文表名注释）——"会写 SQL"从门槛变零，与 ai-regex-generate 组成生成工具批
- **体验审查（v6.7）**：扫描 v6.1~v6.5 全部新组件（PasteGuardDialog / SequenceDiscover / ProfileRefine / ProfileExport / ChainRunnerDialog / ChainEditor）主题适配，确认无硬编码浅色问题

### 内部
- suggest.test.ts 新增 2 个会话编排测试；actions.rs 新增 `test_sql_generate_prompt`
- chainRegistry.test.ts 为 error-triage 引用的动态 AI 动作（ai-merge-polish / ai-fix-code，运行时由 `ai_list_actions` 注册）补桩校验，预置链引用完整性检查对 AI 链同样生效

## [6.5.0] - 2026-08-10

### 新增
- **三闭环互操作（v6.5 收尾：记忆 × 链 × 行为学习）**：
  - **链执行记入行为日志**：动作链每次运行（成功/失败）都记入 action_events（content_type="chain"）——"用了哪些链"进入行为学习，程序性记忆数据补全
  - **记忆 × 编链**：AI 编链时把工作记忆会话拼进输入（截断 800 字）——模型能看出"这是连续内容"，编出的链更贴合任务
  - **记忆 × 运行器**：动作链运行器新增「填入会话（N 条）」按钮——把工作记忆里的连续复制内容一键填进链输入（含条件链场景：把 5 条代码丢给"敏感自动脱敏"链一次跑完）
- 画像 → 场景化建议（v2 已注入推荐排序）、偏好 → 条件链（v6.3）此前已落地，本版完成最后闭环

### 内部
- 链 id 记 action_id 但不参与动作推荐权重（content_type="chain" 隔离）；日志失败不影响主流程

## [6.4.0] - 2026-08-10

### 新增
- **工作流技能包（生态调研 S1）**：把自定义 AI 动作 + 动作链一键打包成 SKILL.md，装进 `~/.claude/skills/pastepanda-workflows/`——Claude Code / Cursor / Codex 等 **26+ 平台可直接调用**你的方法论
  - 画像导出卡新增「导出我的动作与链为 Skill」：含动作模板（`{{内容}}` 占位）+ 链步骤清单 + `references/workflows.json` 快照
  - 你的亲手配置 = 可移植能力包（Agent Skills 标准，7 万 skill 生态同款格式）
- **生成正则动作（ai-regex-generate，生态调研 E）**：粘贴自然语言描述（"把 138 开头的手机号换掉"）→ 输出一行可直接用的正则——把"会正则"从门槛变零

### 内部
- 截图即解释（F）已具备：OCR → 变换枢纽 → AI 动作管线早已打通，无需新增

## [6.3.0] - 2026-08-10

### 新增
- **链条件执行（v6.3 主线，循环 1→5：动作链从线性到分支）**：动作链步骤可以带条件，不满足自动跳过（输出原样传递）
  - 4 种条件（纯本地规则，零 LLM）：无条件 / 是 JSON 时 / 含敏感信息时 / 是代码时
  - 链编辑器每步可选条件（虚线下拉）；运行器跳过步骤显示「跳过（条件不满足）」徽标
  - 新增 2 条示范条件链：**敏感自动脱敏**（含密钥/手机号才脱敏，否则原样）、**JSON 条件格式化**（是 JSON 才格式化）
  - 条件判定放在 AI 确认之前——跳过的步骤不会触发联网确认（红线不变）
- 自定义链持久化兼容 `condition` 字段（后端 ChainStepDef 新增，旧链不受影响）

### 内部
- `matchesCondition()` 单一实现（运行器与测试共用）；跳过步骤不参与失败定位

## [6.2.0] - 2026-08-10

### 新增
- **粘贴守卫（v6.2 主线，感知 8→9：粘贴前是决策点）**：粘贴这个动作从"裸奔"变成有安全意识
  - **敏感内容粘贴前确认**：内容里识别出密钥/手机号/邮箱/身份证/IP（本地规则）→ 粘贴前弹确认条，预览脱敏效果，可选「脱敏后粘贴 / 原样粘贴 / 取消」——复制密钥配置粘到群聊/工单前多一道自己说了算的闸
  - 脱敏在本机完成（规则替换），不上传任何内容
- **目标感知粘贴 v1**：Rust 侧读前台窗口进程名（浏览器/Excel/Word/WPS/IDE/终端）→ 确认条显示「将粘贴到 ××」

### 内部
- `paste_precheck` 命令（Rust 读前台进程，OpenProcess + QueryFullProcessImageNameW）；`pasteGuarded()` 统一入口，主窗口卡片粘贴已接入；批量/连续粘贴保持原样不打断

## [6.1.0] - 2026-08-10

### 新增
- **工作记忆 / 会话桶（v6.1 主线，记忆 6.5→9 的第一步）**：系统第一次"注意到你在拼什么"
  - 90 秒内连续复制的同类内容（代码/文本/markdown/log）聚成一个会话桶——纯内存、不落盘、不出本机、用完即弃
  - 建议条会话感知：连着复制 2+ 段代码 → 「看起来你在拼 N 段内容——要 AI 合并成一份吗？」→ 打开变换枢纽直接合并去重
  - 同内容连贴自动去重、超过 90s 间隔视为新任务、会话被使用/否决即清空
- **修复代码动作（ai-fix-code，生态调研 S3）**：粘贴报错或坏代码 → 输出修复后的完整代码（拿不准保留原样并注释说明），从"解释代码"补上"修好它"这一环

### 内部
- 建议优先级：意图 > top-1 > 序列 > **会话** > 跑链；会话建议与链/意图一样"仅本次收起"（不污染动作表）

## [6.0.0] - 2026-08-10

### ✦ AI 版本（v5 → v6 里程碑）

从 v5.6.7 到 6.0.0，PastePanda 完成了从「剪贴板工具」到「剪贴板 AI Agent」的跨越。
6.0.0 定义为 **AI 版本**，汇集以下能力：

#### AI 底座
- 16 家云端服务商（OpenAI 兼容 / Anthropic 双协议）、自定义 AI 动作（`{{内容}}` 模板）、24h 缓存、敏感内容防护、日预算与用量明细、推理模型 think 剥离
- 本地 OCR → AI 变换管线（图片本身不上云）

#### 懂用户：学习与记忆
- 行为学习闭环：动作使用日志 → 推荐排序 + 负反馈 + 冷启动阈值 + 粘贴价值日志 + 自我净化
- 偏好学习：AI 结果可编辑 → 被改率统计 → 偏好指令自动进 system prompt
- 偏好自荐：识别 8 种修改方向（变短 / 删开场白 / 敬语↔口语…），一键写入偏好
- 剪贴板周报（行为级）、场景感知、主动建议（只给 top-1、否决被记住）

#### 用户画像
- 「我的画像」：角色概率推断（6 维雷达图）+ 内容领域 + 活跃时段 + 风格偏好，纯本地统计、可见可删
- 画像驱动推荐：角色 → 动作加成，推荐排序按画像个性化
- 导出三件套：`profile.md`（5 大类通用格式）/ `profile.json` / `SKILL.md` 技能包，一键装进 Claude Code / Cursor
- LLM 精炼画像：统计画像说成人话，复制给任意 AI 工具即可快速了解用户

#### 有思想：理解与执行
- 意图识别：排错 / JSON / 收集链接 / 批量 / 长文本提炼 / 财务 6 种意图——建议条从「推荐动作」升级为「理解你在做什么」
- 动作链：预置链 + 编辑器 + 持久化 + AI 编链（三道校验不信模型一个字）+ 高频序列自动挖掘（程序性记忆，一键存链）
- 内容记忆：本地检索摘要 + FTS5 中文全文索引 + 云端语义搜索（授权开关默认关，**原文永不出本机**）
- 搜索：自然语言时间词（「上周那个 API 文档」）、语义命中区

### 隐私红线（始终如一）
- **行为可记、内容不存**（内容记忆为显式授权项）；学习日志 / 画像 / 记忆全部可见可删
- 出网全过闸：密钥拦截（is_secret）+ 日预算 + 用量明细，云端 LLM 只负责「生成」，本地负责「存储与索引」

## [5.24.0] - 2026-08-10

画像导出的两条“勾选不算数”。

### 修复
- **把四个大类全取消勾，拿到的却是全量画像**：后端把“空数组”当成了“全部”，恰好和用户的意思相反。现在“不传”才是全部，空数组就是一个都不要；一个都没勾时复制 / 保存 / 安装均禁用并给出提示
- **SKILL 技能包：预览按勾选过滤，落盘却是全量**——`profile_install_skill` 以前没有 categories 参数、写死“全要”。现已补上并与导出**共用同一个过滤器**：预览里的 SKILL.md 就是安装写入的 SKILL.md
  - 过去靠按钮下一句「安装恒为全量、不受勾选影响」的免责声明遮盖此事，现已连带删除
  - 安装确认框改为逐条列出将写入的文件与大类，并明说 `references/profile.json` 始终是完整快照

### 内部
- 大类过滤收成唯一实现 `cat_filter()`（之前导出与安装各写一份，正是两个 bug 的根因），新增 5 条回归测试
- `ConfirmDialog` 正文改为 `white-space: pre-line`，让确认框能排出文件清单

## [5.23.0] - 2026-08-10

一轮全面审查后的修复批次（主要面向 AI 相关的建议、画像、动作链）。

### 修复
- **AI 未配好时，界面会承诺做不到的事**——四处各自绕过了“AI 能不能用”的正确判据，现已**收口为唯一一处计算**（新增 `lib/aiAvailability.ts`）
  - 顶部 AI 胶囊以前只看“开关开着 + 选了服务商”、**不查密钥**，于是切到没存过密钥的服务商后仍显示绿色「AI 就绪」；现在多一个「AI 待配置」态
  - AI 快捷区以前在未配好时照样摆出一排点下去必然失败的按钮（而变换面板里那些动作根本不存在）；现在不可用就不摆
  - 意图建议（“看起来你在排错”）以前会推荐云端动作，点「使用」**完全无反应**；现在主动作不在时整条建议不出现
  - 画像的「生成画像描述」以前在未配 AI 时也可点，点了才报错且无处可去
  - 附带修复：以前**用量统计报错会把 AI 整体降级成未启用**
- **意图识别误报面过大**：任何含“失败/异常/出问题/error”的普通文本都被当成排错（“这次活动失败了”→建议「解释代码」）。现在普通文本需要真的堆栈特征才判定
- 日志行（`[INFO] …`）、引用标记（`[1] …`）、Markdown 链接不再被误判成“一组数据”；shell 的 `$1 $2` 不再被当成金额
- **高频操作序列会拿出根本不成立的建议**：以前不看事件间隔，“下班前最后一个动作”和“次日开工第一个”会被当成一个习惯序列；现在超过会话间隔就断开
- **序列存成链几乎必失败**：自动生成的链名没算前缀长度，常见序列一点「存成链」就超名称上限，而那里没有改名入口。现已修正并**新增就地改名**
- 序列建议里引用已删除自定义动作的不再提示（以前会存出一条跑不通、且被标成“全部本地”的链）
- “忽略”过的序列建议不再每次重开就又冒出来
- **零步骤的链以前会“成功”**（什么都没做却报告完成），现在明确报失败，并区分“没配步骤”与“配置已损坏”
- **雷达图在 4/6 套主题下基本不可读**：7 处写死颜色是照着深色主题调的，浅色主题下网格线完全消失、轴标签几乎看不见。已全部改为主题变量
- 导出面板的“包含”勾选语义反向（一个不勾 = 全部，勾一个反而变少）；现在默认全勾并标注说明
- 导出预览失败以前被静默吞掉（退化成“像是还没选”，而“实时生成”的绿灯还一直亮着）
- 画像描述生成失败以前只有一条会消失的 toast；且已付费的结果误点遮罩就永久丢失
- 链名重名校验失败时会把 SQLite 英文原文报给用户（测试里已有断言禁止这事，但守卫本身有洞）
- 领域占比之和明显小于 100（先对每个内容类型取整、再合并同领域，误差累积）

### 安全
- **画像发云端之前无二次确认**，而同一个弹窗里就写着默认红线“云端请求先确认”。现改为两段式（先变「确认发送 →」并展示服务商/模型）
- **“只发送行为统计（不含内容）”的措辞比实际范围窄**：你手写的风格偏好会原文发出（仅剔除疑似密钥的）。已改成诚实措辞
- 安装画像 skill 会静默覆盖已有文件（无备份、不进回收站），现在先弹确认并说清写到哪里
- 界面上的大类勾选对「装进 Claude Code」完全无效（取消勾选“使用红线”后手写红线照样落盘），现已明写提示
- 画像统计以前不排除 abandoned 结果（文档声称排除但 SQL 没写）；上游一旦开始记弃用事件，**越放弃画像会越“自信”**
- 链步骤的风险级别推导收归到唯一实现（序列发现处原有第二份）——运行器靠它告诉用户“这条链会做什么”

### 变更
- AI 快捷区：计费动作与免费本地动作现在外观可区分（以前只差在 tooltip）；本地动作不再显示“AI 思考中…”
- AI 快捷区的错误条现在**一律**带去设置的出口（以前只有预算类有，而且是按文案里有没有“预算”二字硬判）
- 首次引导补上代价告知：会联网、内容发给第三方、按用量计费与默认每日上限（以前“会花钱”在第一次使用前从未被明说）
- 链编辑器：保存前校验步骤引用的变换是否存在（这条分层约定以前前后端两侧都是空的）；含已失效变换的链会明确标出而不是下拉空白
- 修正 8 个样式文件里 36 处引用的 `--text-tertiary`——**这个变量全项目从未定义**，导致这些“三级弱化说明”实际渲成与正文同色（受影响的正是“出网计费”这类最该被读到的提示）
- 修正多处实心主按钮的白字对比度（`--accent` → `--accent-solid`）
- 序列挖掘不再全程持有数据库锁做纯计算（以前会瞬时阻塞剪贴板写入）

## [5.22.0] - 2026-08-10

### 新增
- **偏好自荐（自进化闭环的最后一跳）**：以前系统只知道“这个动作的输出你常改”，但不知道你改成了什么，所以只能提醒你去手动写偏好。现在它看得出**方向**
  - 同一个动作上用同一种改法改了 3 次，结果区下方出一条提示：“你常删掉开头的‘以下是…’——要不要记住？”，点一下写入动作偏好，之后每次调用自动带上
  - 能认出 8 种方向：变短 / 变长 / 删开场白 / 删称呼问候 / 删结尾客套 / 去 Markdown 标记 / 敬语↔口语
  - **全程不存内容**：对比在本地做，落库的只有枚举标签（命令层再校一次白名单，非法值一律丢弃）；否决被永久记住，但只针对那一条方向，同一动作的其他方向仍会问
  - 信号 90 天自动过期，「系统学到了什么」里可一键清空（红线②）
- **AI 编链**：变换枢纽顶部新增「AI 编链」——让模型看一眼内容，自己组一条多步流水线
  - 编完直接进动作链运行器并预选（带 AI 标记），**你看完步骤再点运行**；满意可一键「存为我的链」变成普通自定义链
  - 只发内容前 1500 字（判断类型不需要全文，既省钱也少传），同样过出网闸 / 日预算 / 用量记账
  - 手动触发，不自动编——自动编链 = 每次复制都调一次云端，而十次里有九次用不上

### 安全
- **AI 编的链不信模型的任何一个字**（三道校验，均带回归测试）：
  - 动作 id 必须在变换注册表里，模型编造的一律丢弃并告知用户（静默丢弃会让人以为模型就是这么编的）
  - 步骤风险级别一律本地推导，不采信模型——否则它把联网步骤标成“本地”就绕过了发送前确认
  - 执行类动作（开浏览器 / 开目录 / 起邮件）不得进链，避免模型编的计划直接产生副作用

### 变更
- 链步骤的风险级别推导收归到单一实现（链编辑器与 AI 编链共用）
- 路线图删除原 v7.0「操作系统层」（MCP server + 变换注册表插件点）——方向错误，不做；功能清单的 F3 同步删除

## [5.21.0] - 2026-08-10

### 修复
- **点了建议条的 ✕ 却告诉你「不再建议」，下次它又出现**。任务类（意图）与链类建议按设计只本次收起、不入动作表，但文案没跟着分岔。现在分清：真的记住了才说「不再建议」，否则只说「已收起」（主窗口与托盘弹窗两处）
- **自建的中转服务可能直接 400、整个 AI 不可用**。自定义服务商被当成支持「关掉思考」，于是请求里被塞进了 DeepSeek 的开关字段（厂商表认不出的 id 会回退到默认厂商）——中转服务不认识该字段就报错。现在只向查实过写法的内置厂商发，界面也不再给自定义服务商摆这个开关
- **AI 反馈数据永久留存**。保留天数定义了但从未生效，而用量明细与动作日志都有 90 天自动清理。现在三者一致——“可见可删”不能只给一个清空按钮，不自动过期就等于默认永久保存
- **用户自建的动作链从不被主动建议**，只推荐自带的预置链。现在自定义链一并参与，且同分时优先

### 安全
- **动作链里的 AI 步骤改为默认拒绝**：之前“发送前得先确认”这个检查靠调用方传回调，没传就等于跳过。现在没有确认回调就一律不执行，把“云端内容不自动发送”从约定变成强制（带回归测试）

### 新增
- **LLM 精炼画像（V3-C，画像消费收官）**：统计画像升级为"像人话"的画像描述
  - 「我的画像」新增「AI 画像描述」区：一键把角色概率 / 领域分布 / 高频动作 / 活跃时段 / 风格偏好润色成 2~3 句连贯描述，可复制给其它 AI 工具
  - 手动触发（按钮）、只发纯统计值（后端过敏感清洗 + 日预算 + 用量记账）、样本 <10 条时拒绝生成

## [5.20.0] - 2026-08-10

### 新增
- **程序性记忆（V3-B，"自我进化"的一环）**：从动作使用日志挖掘高频操作序列，固化"你惯常的多步操作"
  - 动作链运行器顶部新增「发现你的高频操作」：最近 30 天出现 ≥3 次的连续动作序列（2~4 步），自动提示一键存成动作链
  - 与已有链（自定义 + 预置）自动去重，已存在的序列不再提示；存链后步骤自动标注风险（AI 步骤 → 联网）
  - 纯行为统计（只记动作顺序，不含任何内容），守红线②

## [5.19.0] - 2026-08-10

### 新增
- **意图识别（V3-A，"有思想"第一步）**：主动建议从"推荐单个动作"升级为"理解你在做什么"
  - 六类意图识别（纯规则、零出网）：**排错**（报错内容 → 解释代码→提取要点）、**JSON/结构化**（JSON → 生成类型/格式化）、**收集链接**（连续复制链接 → 摘要）、**批量数据**（3+ IP/邮箱/手机号 → SQL IN）、**长文本提炼**（500+ 字 → 总结要点）、**财务**（金额数据 → 表格）
  - 建议条优先显示意图级建议（"看起来你在排错——要连着「解释代码 → 提取要点」一起做吗？"），点一下执行主动作
  - 主窗口与托盘弹窗同步生效；意图否决仅本次收起（不污染动作表）
- **画像驱动推荐（V3）**：推荐排序叠加画像角色加成——判定你是"开发者"后，「解释代码 / JSON 格式化 / SQL IN」自动优先；"运营"则周报/表格类优先（角色置信 ≥35% 才参与，同一动作取各角色最大值防雪球）

## [5.18.0] - 2026-08-10

### 新增
- **我的画像（用户画像）**：「系统学到了什么」新增「我的画像」入口
  - 实时聚合最近 30 天行为，纯规则推断（零 LLM、零出网）：**职业角色**（开发者/研究/文案/沟通/运营/数据，概率分布）、**内容领域**（代码/链接/文本…）、**高频动作**、**活跃时段**、**风格偏好**（偏好指令 + 被改率）
  - 样本不足自动标注低置信度；角色可手动修正（覆盖自动推断）；可补充自定义红线
- **画像导出（可移植资产）**：一键导出带进任何 AI 工具
  - `Markdown`：5 大类通用格式（Profession / Projects / Preferences / Instructions），任何 LLM 都能解析
  - `JSON`：结构化画像（含角色概率与置信度）
  - `SKILL 技能包`：可直接安装到 Claude Code / Cursor / Codex 的 `pastepanda-profile` 技能
  - 大类可勾选、生成前预览、复制或保存文件；**导出仅含统计值，不含你的内容**，敏感信息自动清洗

## [5.17.0] - 2026-08-10

### 新增
- **AI 记忆增强（语义搜索）**：AI 高级设置新增开关（默认关）。开启后，历史摘要会通过当前 AI 厂商生成语义向量、**存在本地**做余弦检索——搜"上周那个 API 文档"这类凭印象的查询能按**意思**命中，而不再只按字面
  - 语义命中结果展示在搜索结果上方（相似度百分比 + 摘要预览 + 一键复制/粘贴全文）
  - 摘要/搜索词发送给 AI 厂商计费（受日预算约束），**原文永不出本机**；搜索词含疑似敏感内容会被拦截
  - 索引懒构建：搜索时自动补齐，设置页也可手动「立即建立索引」
  - 关闭开关自动退回关键词搜索，功能不缺失；「系统学到了什么」可查看语义向量条数，清空内容记忆时一并清除
- 服务商 embedding 支持：OpenAI / 智谱 / 通义 / 硅基流动 / 火山 / 混元（可自定义模型名覆盖）

## [5.16.0] - 2026-08-10

### 新增
- **内容记忆**：为历史内容生成本地检索摘要（提取链接域名、邮箱和正文开头）——凭印象搜索更容易命中（比如只记得"那个 docs.rs 的链接"）。摘要只存本机、敏感内容（密钥等）不会记，可在「系统学到了什么」里查看条数并一键清空
- **AI 设置面板重做**：服务商从下拉改成卡片网格（15 家内置 + 自定义分区，带"已配置/本地"角标）；**切换服务商不再丢配置**——每家的模型/地址/协议独立保存，切走切回原样带回（修复了之前"切回来配置被清空"的问题）
- **自定义服务商支持多个**：可以添加多个中转/代理服务（各自独立的名称、地址、模型、密钥），添加后自动切换过去；可随时编辑或删除（删除连密钥一起清掉）
- **AI 设置页整体美化**：模型清单折叠为"常用 + 更多"、用量卡升级为环形进度、用量明细加汇总条与表头、高级区控件统一（协议切换/开关）、密钥与测试结果状态一眼可辨
- **AI 动作体验统一**：AI 运行中显示"AI 思考中…"（本地动作保持原样）；结果区模型与缓存命中用彩色角标标识；被截断的回答用醒目的黄色卡片提示去调大上限

## [5.15.0] - 2026-08-10

### 新增
- **建议更懂你的口味**：如果一个动作的结果你经常改（比如翻译总改短），建议条就不会再反复推它，直到你在「系统学到了什么」里给它写了偏好指令
- **建议用「动作链」一次跑完**：复制的内容如果适合某条链（如一段带标签的网页 →「网页 → 纯文本」链、含手机号 →「敏感信息脱敏」链），建议条会提示"用「××」链一次跑完（N 步）"，点一下直接打开运行器并预选这条链

## [5.14.0] - 2026-08-10

### 新增
- **AI 结果可以直接改**：翻译/摘要/改写等 AI 动作的结果上多了「修改」按钮——点开变编辑框，改完复制或粘贴的会是你改过的版本
- **AI 结果反馈**：你改过 AI 产物 = 对输出不满意的信号。「系统学到了什么」里新增「AI 结果反馈」区——能看到每个动作的结果被改过多少次（被改率），也能一键清空
- **动作偏好指令**：在「AI 结果反馈」里给某个动作写一句偏好（如「译文更简洁」「不要译称呼」），之后每次调用该动作都会自动带上——越用越贴合你的口味。只存本机、不存内容，改偏好会自动清除旧缓存

## [5.13.0] - 2026-08-10

### 新增
- **自定义动作链**：动作链运行器里新增「新建链」——从变换列表选步骤、排序、命名保存，以后一键跑自己搭的流水线（如"剥标签 → 翻译 → 精简"）。自己建的链会排在官方链前面，悬停可编辑或删除
- **动作链支持 AI 步骤**：自定义链里可以放翻译/摘要/改写等 AI 动作，运行到该步会先问一句"会把内容发送到云端（可能计费）"，确认后才执行——云端内容不会静默发送

## [5.12.0] - 2026-08-10

### 新增
- **动作链：把多步粘贴流程串成一个按钮**：变换面板顶部新增「动作链」入口，内置 4 条官方链——网页 → 纯文本、JSON 清洗并格式化、敏感信息脱敏、HTML 实体解码并清理。点「运行整条链」，每步结果都会展示出来（可展开对比这一步的输入和输出），失败会明确到第几步并保留失败前的中间结果，最后复制或粘贴到前台
- **JSON 格式化**：变换列表新增「JSON 格式化」动作（校验并缩进美化），单独可用，也是动作链的组成部分

## [5.11.0] - 2026-08-10

### 新增
- **回复草稿一次给 3 个语气候选**：复制别人发来的消息 →「回复草稿」→ 会同时给出正式版、简洁版、轻松版三份候选，各自独立预览、一键复制或粘贴，挑最顺手的用。态度选项（答应/委婉拒绝/追问细节）照旧生效

## [5.10.1] - 2026-08-10

### 新增
- **直接说想做什么**：变换面板顶部新增指令输入框——输入「改得正式一点」「翻译成英文」「总结要点」，自动定位到对应变换并预填参数（翻译可指定语言）。本地智能识别，不联网
- **托盘弹窗也会主动建议**：点开托盘图标，最近复制内容如果明显适合某个动作，弹窗顶部会显示一条建议（如「用 SQL IN 处理这段内容？」）。点「使用」直接出结果并粘贴到前台，点 ✕ 记住不再建议
- **链接摘要**：复制链接（文章/文档/网页），变换面板会出现「链接摘要」——自动抓取页面、提取标题和正文；启用 AI 后自动生成更精炼的要点版。只存摘要不存原文
- **粘贴脱敏**：复制带密钥/手机号/邮箱/身份证/IP 的内容，变换面板会出现「粘贴脱敏」——先预览脱敏版（保留可辨识前缀），确认后再复制或粘贴，避免误伤
- **多选合并**：勾选多条历史记录 →「合并」→ 选分隔方式（换行/逗号/编号等）→ 预览后粘贴或复制；启用 AI 后可一键"去重 + 顺排 + 润色"
- **剪贴板周报**：设置里新增周报入口——本周复制/粘贴的行为统计（内容类型、来源、时段、常用动作、粘贴转化率），启用 AI 后一键生成周报文字。只统计不读内容

### 改进
- **推荐更懂"场景"**：推荐排序会参考你当前的使用环境——工作时段在代码编辑器里复制，偏工程的变换（如 SQL IN）会优先；晚上在浏览器里复制，阅读/翻译类变换会更靠前
- **搜索更快了**：历史搜索改用全文索引，中文关键词也能精准命中；输入「上周/今天/本月」开头的搜索会自动带上对应时间过滤
- **AI 兜底推荐**：内容本地规则判断不出该用哪个变换时，AI 相关变换会出现在推荐区（仅启用 AI 后）
- **主动建议同步感知场景**：复制后的提示建议也会按当前时段和来源应用调整

### 修复
- **带思考的模型（如 Agnes-2.5-flash）接入不再报错**：这类模型会先把额度花在"思考"上，之前测试连接时给它的额度太小，思考就占满了、答案一个字都生成不出来——现在测试和动作都给思考留足了空间
- **内置动作与示例模板的输出上限整体调大**：摘要、解释代码、提取要点、写 commit 等动作，之前的上限对带思考的模型不够用；现在统一加大，不会再把回答截断在"思考了一半"的地方

## [5.9.0] - 2026-08-09

### 新增
- **主动建议（第一期）**：打开主窗口时，新复制的内容会触发**一行**贴心提示——
  - 内容明显适合某个动作时（比如一段 JSON），提示「用 SQL IN 处理这段内容？」；连续复制 3 个 IP 或邮箱时，提示「把 3 个同类内容合并成 SQL IN？」
  - 点「使用」直接打开变换面板并定位；点 ✕ 就再也不提这个动作（会被记住）
  - **克制原则**：不弹窗（只在主窗口里显示）、一次只给一个建议、8 秒无操作自动收起

## [5.8.1] - 2026-08-09

### 新增
- **「保护常用内容」开关（设置 → 清理过期记录）**：默认开启——打标签 / 粘贴过 / 搜索找回过的内容不参与自动清理。不想让系统"自作主张"的话可以关掉，完全退回"超期必清"的旧行为，设置页的过期数量会相应变化

### 改进
- **自动清理开始"按价值"取舍**：清理过期记录时，被打过标签、被粘贴过、或被你搜索找回过的内容会**永久保留**，不再因为超期被清掉；只有从未被用过、来源不明的"过期临时内容"才被清理。设置页显示的过期数量与清理数量保持一致
- **搜索记录命中**：每次搜索找回某条记录都会记一笔（仅本机），作为"这条内容对你有价值"的证据

## [5.8.0] - 2026-08-09

### 新增
- **推荐排序开始学习你的习惯**：变换中心里的动作顺序不再对所有人一样——你常用的动作会自动排到前面（比如你总在 JSON 内容上用「SQL IN」，它就会越来越靠前）。数据只存本机、不包含任何复制内容本身
- **「不再推荐这个」**：变换卡片悬停时出现 ✕ 按钮，点一下某个动作就不再出现在推荐里（也可以去设置里查看和撤销）
- **设置新增「系统学到了什么」**：能看到近 30 天使用次数、最常用的变换、已「不再推荐」的清单，并可一键清空全部学习记录——清空后推荐退回默认排序
- **粘贴也会被记入使用日志**：用于后续「按价值清理」——从没被粘贴过的内容会被优先清理，经常用的会保留

### 改进
- **冷启动保护**：使用数据攒够之前（约 20 次操作），排序保持原来的默认推荐，不会给新用户一个「还没学会」的排序

## [5.7.0] - 2026-08-09

### 新增
- **复制即执行**：鼠标悬停历史记录时，卡片上会直接出现当前内容可用的快捷动作——复制的是网址就显示「打开链接」，是本地路径就显示「在资源管理器打开」，是邮箱就显示「发邮件」，是 IP 或域名就显示「查询归属」。点一下直接执行，不用再打开变换面板
- **执行类动作进入变换中心**：这些动作不产出文本，而是直接执行（打开浏览器 / 定位文件 / 写信 / 查 whois）。卡片上只有「执行」按钮，点一下生效
- **打开链接更安全**：只会执行 http/https/mailto 这类常规链接，剪贴板里混入的 `file:`、`cmd:` 等其他协议会被拒绝，不会因为复制了一行可疑内容就把本地程序打开

### 改进
- **动作使用日志覆盖执行类动作**：在变换中心或卡片上执行动作也会被记录，为后续「推荐排序越来越懂你」积累数据（仅存本机）

## [5.6.7] - 2026-08-08

### 改进
- **开始记录你在「变换为…」中的使用习惯**：哪些变换经常用、在什么内容上、什么时段用，只存在本机、不包含任何复制内容本身。后续版本会用这些数据让推荐排序越来越贴合你的习惯，而不是对所有人一套排序

## [5.6.6] - 2026-08-08

### 修复
- **用推理模型时，AI 变换返回的是一大块“让我想想……”而不是答案**。推理模型（如 MiniMax-M3）会把思考过程包在 `<think>` 里一并返回，现在会被剥掉，只留真正的回答
- **回答被 token 上限截断时会明确告诉你**，而不是把半截内容当成正常结果。若额度全花在思考上、答案一个字都没生成，会直接说清楚该把上限调到多少
- **截断的结果不再进缓存**。之前会被缓存 24 小时，且因为命中缓存不花钱也不报错，重试多少次都一模一样，看起来像是“本来就这样”

### 优化
- **云端动作的徽标从「联网」改成「AI」**。原来的徽标看不出这是 AI 能力，而“会发到云端”这件事卡片里本来就写了一遍（“点运行后才会发送到云端”），等于把徽标这个位置花在了重复信息上
- **默认关掉模型思考，更快也更便宜**。DeepSeek v4、智谱 GLM-4.7、MiniMax、通义千问 3.5+ 的新模型都是**默认先输出一大段思考**，而剪贴板动作多是短产物，那段思考几乎是纯成本（实测一次“精简一半”花掉 13.8 秒与 1500 输出 token，而答案不到 20 字）。需要深度推理时可在「高级设置→关掉模型思考」里关掉这个开关
- **模型选择里会标出“会先思考”的档位**，并提醒思考的 token 照样计费、也照样占用动作的 token 上限（建议给到 3000 以上）

## [5.6.5] - 2026-08-08

### 新增
- **图片现在也能用变换与 AI 了**：右键截图 →「变换为…」会先用**本地** OCR 把文字识别出来，再把它交给整套变换——翻译、解释报错、提取要点、你自己写的动作都直接能用，不用为图片另学一套
- **识别结果会先摆出来给你看，而且可以改**：识别错了就没必要花那个钱。**图片本身不会被发送**，只有那段文字会
- **图片预览里框选一块，也能直接「变换为…」**：只想处理截图里的某一段时不用先复制再粘。两个入口共用同一个动作列表，不会出现“这边有那边没有”
- **OCR 不受 AI 总开关影响**：它是本地引擎，不联网不花钱。关掉 AI 后仍然能识别并复制文字，只是不再有云端动作可选
- **自定义 AI 动作**：写一段提示词模板就多一个变换，不用等发版。模板里用 `{{内容}}` 标出剪贴板内容的位置（可以放在中间，写“先规则 → 内容 → 再强调一遍”的夹心结构）。可指定它只在哪些内容类型上出现，与内置动作**同处一个列表、同一套排序**
- **新建时可从示例开始**：提取待办 / 起变量名 / 翻译成日文 / 精简一半 / 写成正式邮件 / 解释报错——每个都自带示例输入，套用后一点就能看到效果
- **编辑器里能直接试跑**（不用先保存）：写 prompt 本来就要迭代，“保存→关闭→去变换中心试→回来改”这个循环没人会走完。试跑**会真实计费**，按钮上写明了，也计入用量明细
- **内置动作从 4 个增到 10 个**：新增润色纠错、提取要点、写 commit message、JSON → 类型定义、表格化、回复草稿（可选答应/委婉拒绝/追问细节）。它们都绑定了适用内容类型，不会在无关内容上冒出来
- **内容类型会一并告诉模型**（“这是一段 JSON / 代码 / 日志”），翻译代码注释和翻译邮件的表现不再一样。**只发类型，不发来源应用名**——后者会泄露“你在用什么软件”
- **变换中心新增「AI」分组**：翻译 / 一句话摘要 / 解释代码 / 改写语气。它们和原有变换同处一个面板、同一套推荐排序，不是另开一个新界面
- **云端动作不会自动执行**：带「AI」徽标的动作需要你点“运行”才会把内容发出去，打开面板本身不会产生任何请求或费用；改完选项后需要重新运行
- **内容看起来是密钥时会先拦下来**，确认后才发送
- **设置新增「AI」标签页，主流程只有三步**：选服务商 → 选模型 → 粘 API Key →「保存并测试」。地址 / 协议 / 超时 / 费用上限在折叠的高级区。**默认关闭**，不配密钥就不会出现任何 AI 入口
- **每家服务商预制了模型档位**（便宜快速 / 均衡 / 最强），芯片形式直接摆在主流程里。不动手就用第一档（便宜快速），点一下就能换。**所有服务商都能手填模型名**——芯片下方的输入框常驻，厂商出了新模型不用等我们发版
- **模型清单按官方文档刷新**（DeepSeek V4 系列、通义千问 3.7/3.8、智谱 GLM-4.7/5.2、Kimi K2.5–K3、MiniMax M2.7/M3、GPT-5.6、Claude Haiku 4.5 / Sonnet 5 / Opus 5）
- **支持 16 家服务商**：DeepSeek、通义千问、智谱 GLM、Kimi、豆包（火山方舟）、腾讯混元、百度千帆、讯飞星火、阶跃星辰、零一万物、MiniMax、硅基流动、Ollama（本地）、OpenAI、Anthropic Claude、自定义中转。每家都带**申请 Key 的直达链接**
- **两种接口协议都支持**：OpenAI 兼容（`/chat/completions`）与 Anthropic Messages（`/messages`），且可在高级区单独覆盖——同一家厂商常常两种都提供（如智谱），中转服务更是如此
- **密钥按服务商分开存**：换一家试试、再换回来，不用重新输入密钥；下拉里会标出哪些已配置
- **测试通过就自动启用**，不再要求你测完再自己去找开关
- **错误提示带下一步**：密钥错→一键回到输入框；被地区封锁（403）→一键换国内厂商；路径/协议不对（404）→一键展开高级设置
- **每次调用的 token 消耗都有记录**：设置 → AI →「用量明细」可看近 7 天统计、按动作的花费分布、最近 50 次调用的时间/模型/token/耗时，以及缓存命中率。**只记 token 数，不存任何内容**；只存本机、保留 90 天、可一键清空
- **费用改用人民币显示**，并告诉你“预算内大约还能再做多少次”——按你自己的历史均值算，不是拍个固定数

### 修复
- **MiniMax 接口地址纠正**：以前写的是已不在文档里的 `api.minimax.chat`，现在国内站是 `api.minimaxi.com`（带 i）。旧地址会让配置白白失败
- **Kimi 预制模型避开即将下线的型号**：旧的 `moonshot-v1-*` 系列官方公告 8 月 31 日下线，已换成 K2.5 / K2.6 / K3
- **硅基流动不再预制模型**：聚合平台的上下架与免费名单变得很快，写死几个只会在它们下架后变成一键 404；改为提示从模型广场拷贝型号
- **「敏感内容防护」现在认得出主流 AI 与云服务的密钥**：以前开着这个开关，复制 OpenAI / Anthropic（`sk-` 开头）、Slack、Google API、GitLab 的密钥依旧会被记进历史——这类密钥带连字符，而旧规则只认纯 Base64 长串，正好全部漏掉。现在它们会被拦下，不入库、也不会出现在列表里
- **SSH / RSA 私钥也会被识别**：复制 `-----BEGIN ... PRIVATE KEY-----` 开头的私钥块同样不再落入历史（证书不受影响，它本来就是公开的）
- 关闭防护开关时，上述内容会正常记录并多一个「密钥」标签，便于筛选和清理

### 技术
- **自定义动作走独立表 `ai_custom_actions`**，不进 `config` 表：后者每次保存都会触发全量备份并保留 10 份，而编辑动作是高频操作，进去就把备份位全冲光了
- **缓存键拌入了模板哈希**：自定义动作的 id 在编辑前后不变，不拌的话改完模板再跑会直接命中旧结果，用户会以为自己的修改没生效
- **模板必须包含 `{{内容}}`，缺了直接拒绝保存**：若允许缺失并自动拼在末尾，“只输出结果本身”这类收尾约束就会被内容挤到前面，模型很容易照旧带前言
- 变换注册表新增 `unregisterTransform`：删掉或停用一个自定义动作后必须真注销，否则旧的会留在表里，点下去报“未知的 AI 动作”
- `actions.rs` 保持**无状态**：模板由命令层从库里查好传进去，没把 `DataStore` 拖进去——它那一整套单测都建立在纯函数上
- 模板另限 2000 字符（与内容的 8000 分开算）；密钥（secret）**故意不在可选内容类型里**——不应该存在一个专门在密钥上冒出来的云端动作
- **云端 AI 地基（阶段 B0，尚未对用户可见）**：新增 `src-tauri/src/ai/` 模块——厂商预设（DeepSeek 默认 / 智谱 GLM / Kimi / OpenAI / 自定义）、OpenAI 兼容的 `/chat/completions` 调用与错误归一、连通性测试命令。默认厂商选 DeepSeek 而非 OpenAI 是因为国内直连 `api.openai.com` 多半不通，默认值必须是拿起来就能用的那个
- **API Key 用 DPAPI 加密存独立文件，不进 `config` 表**：`save_config()` 每次调用都会把全量配置明文写一份到 `config_backups/` 并保留 10 份，Key 若进那张表就会“明文散 10 份、换 Key 后旧 Key 不退休”。命令层只有 set/has/clear，**没有 get**，密钥永不回读给前端
- HTTP 客户端选 reqwest + rustls（非 native-tls），免掉 OpenSSL 构建依赖
- **云端动作与出网闸（阶段 B1，尚未对用户可见）**：四个动作（翻译 / 一句话摘要 / 解释代码 / 改写语气）+ `ai_run` 命令。**内容被 `is_secret` 判定为密钥时不发送**，而是返回待确认状态——同一套判定在这里管的是“要不要出网”，在剪贴板监听里管的是“要不要入库”
- **日预算与结果缓存**：超预算拒绝调用；同一内容 + 同一动作 + 同一选项 24 小时内直接返回缓存，不重复计费
- **厂商表从枚举改为表驱动**（`ai/provider.rs`）：加一家只需追一行，不用改任何 match 分支。地址逐家用假 Key 实测过（401 = 路径正确，404 才是路径错）；Ollama 与 Anthropic 官方端点未能验证，已在注释里写明
- **403 与 404 单独分类**：实测 Anthropic 官方端点在国内**任何路径都返回 403**（边缘地域封锁，在鉴权之前就拦了），归到“密钥无效”会让用户白白去检查密钥；404 则是协议选错的典型症状
- **用量改存 SQLite 的 `ai_usage_log` 表**，废弃早先的 `ai_usage.json`（启动时自动删除）。两个数据源迟早对不上，而且旧文件只有当日汇总、答不了“每次用了多少 token”。**该表没有内容字段，且有测试锁住**——存了就等于多一份用户不知情的剪贴板历史
- **失败与缓存命中都记账**：不记失败就对不上服务商后台的数；缓存命中记成本 0，它是证明缓存到底省了多少钱的唯一数据。但**均价只能拿真实计费的次数当分母**，把免费的掉进去会把“还能做 N 次”系统性放大（有专门的回归测试）
- **汇率只在后端乘一次**：前端拿到的就是人民币终值，避免出现第二个汇率常量
- **修正一个会让 Ollama 永远不显示的缺陷**：可用性判定原本一律要求已配密钥，而本地厂商根本不需要密钥
- `AiTab` 拆为 `settings/ai/` 下的 4 个子组件，均符合项目规则 #7 的 300 行上限
- **变换注册表新增 `remote` 标记与 `ai` 分组**；AI 动作的定义（名称/选项）以后端为单一数据源，启动时从 `ai_list_actions` 拉取后注册，前后端不再各维护一份
- **修正一个若直接注册就会造成的缺陷**：变换卡片原本挂载即执行以渲染预览，本地变换无所谓，但云端动作照此处理等于“用户什么都没点，内容已经发出去并计费”；现在 `remote` 变换改为待命令状态
- 复制/粘贴改用卡片**已算好的产物**，不再重跑一遍变换（本地是白费计算，云端是多一次往返）
- `TransformHubDialog` 由 319 行拆到 197 行（卡片本体移到 `transform/TransformCard.tsx`），重新符合项目规则 #7 的 300 行上限
- 输入超过 8000 字符**直接拒绝而非静默截断**：截断会悄无声息地改变语义（翻译掉一半、摘要漏掉后半段）而用户看不出来
- `is_secret` 新增服务商前缀表（`sk-` / `xox*-` / `xapp-` / `AIza` / `glpat-` / `gh?_` / `github_pat_`）与 PEM 私钥块判定；原有两条通路要求字符集为 `[A-Za-z0-9+/=]` 且长度为 4 的倍数，带连字符的密钥两条都进不去。前缀命中还要求内容单行且无空格，避免把以前缀开头的说明文字误判为密钥
- 该函数同时是 `should_skip_sensitive_with` 的判定依据，所以这是录入拦截而非仅仅漏了个标签；新增 4 组单测覆盖命中、最小长度、单行约束与 PEM/证书区分

## [5.6.4] - 2026-08-07

### 新增
- **文档保真采集**：从 Word、Excel、PPT、网页等复制带表格、标题、列表的内容时，除了纯文本还会把格式结构一并存下来（卡片带 📄「文档」标记），下次粘贴出去表格与排版不丢
- **文档编辑器**：双击文档卡片打开，原文 / 清洗 / Markdown 三个视图——原文看渲染效果，清洗看去掉排版噪声后的内容，Markdown 可直接编辑保存；整段内容可一键转成 Markdown，表格保真输出（合并单元格自动降级为普通表格）
- **变换中心新增文档类变换**：清洗 HTML、转 Markdown、单独提取表格；另有 PDF 复制文本的断行修复
- **设置新增两个开关**：「文档保真采集」（关闭后复制只存纯文本）与「保留格式粘贴」（控制粘贴时默认带不带格式）

### 改进
- **粘贴文档内容更干净**：把文档条目粘贴到别处时，会先清掉 Word 等来源夹带的排版噪声，不再把看不见的样式带到目标应用
- **快捷粘贴面板认出文档条目**：类型筛选、图标与名称里都能看到「文档」
- **变换中心的自动推荐更容易命中**：PDF 复制文本、Tab 分隔表格的识别门槛放宽
- **在更新弹框里点“下载并更新”后弹框会直接关闭**，不再挡着主界面；下载进度看右上角的圆环徽标（带百分比与速率），下载完成后它会变成「重启」按钮

### 修复
- **「跳过敏感内容」开关显示关闭时实际仍按开启生效**：首次启动未保存过设置时两边不一致，现已对齐
- **下载更新的进度终于是准的**：以前百分比全程卡在 0%、最后一下跳到完成，下载速度也是乱跳（甚至出现负数）、已下载大小一直不动；现在进度、速率、已下载量都能正常增长

### 技术
- 更新下载进度：后端进度回调的第一个参数是本次 chunk 的字节数（`on_chunk(chunk.len(), content_length)`，tauri-plugin-updater 2.10.1），原实现当成累计下载量发给前端，于是 `downloaded/total` 算出约 8KB/7.6MB ≈ 0%；现在在后端累加后再 emit，累加器建在重试闭包内部以保证每次重试从 0 开始
- 弹框按钮不再兼作进度条（弹框在下载期间已不可见，那套渲染分支成了死代码），进度与重启入口统一由 TopBar 的 UpdateBadge 承担

## [5.6.2] - 2026-08-06

### 修复
- **国内加速镜像失败时不再报假成功**：之前镜像推送失败时日志照样打印“已镜像成功”，导致国内下载/更新通道已经坏了却一直没人发现；现在只有真正推送成功、并且回头验证过文件可下载才会报成功

### 技术
- 发版流程的 Gitee 镜像步骤改用 `continue-on-error` 实现“可选”：原先靠 `try/catch` 里的 `exit 0`，但 PowerShell 的 `catch` 接不到 git 这类原生命令的非零退出码（已实测确认），导致“失败却打印成功”与“可选步骤把整个发版弄红”两种错局同时存在
- 镜像步骤新增诊断输出：目标仓库、待传文件清单与总体积、pack 体积、HTTP 层详细日志、每次 push 的耗时/均速/退出码；失败后自动用 `http.postBuffer` + `--no-thin` 重试一次
- 镜像成功后增加可用性冒烟：直接请求客户端实际使用的 raw 地址（含跨域 302 跟随），“推成功”不再等同于“能用”

## [5.6.1] - 2026-08-06

### 修复
- **只复制一张图片时不再被误标成「图文」**：从企业微信等应用复制纯图片时，它们会连带写入一段只含图片的 HTML，导致条目被当成图文混排；现在只有同时含文字和图片才算图文
- **复制图文条目时图片不再丢失**：卡片上的复制按钮、悬停预览、右键菜单这三处复制都只带走了文字，现在图片一起复制
- **图文全屏编辑内容多时能正常滚动**：以前内容超出窗口高度后既不滚动也看不到后面的部分
- **搜索历史保存失败时不再悄悄忽略**：本地存储写满或受限时会记录警告，便于排查“重启后搜索历史没了”

### 改进
- **「图文」变成可点击筛选的标签**：与其它自动标签统一管理——点卡片上的「图文」即可筛选，也会出现在筛选标签列表里（以前只是卡片上一个不能点的徽标）
- **「图片」筛选同时包含纯图片与图文混排**：图文不再单独占一个顶部标签页（默认窗宽下 6 个标签页过于拥挤），只想看图文时点「图文」标签精确筛
- **卡片标签顺序调为「来源应用 → 类型」**：先看到内容从哪来，再看是什么类型

### 技术
- 复制分派逻辑收敛为单一实现（原先在三处重复，新增内容类型时三处全漏）
- 「图片」筛选口径（含图文）在前端本地过滤与后端 5 处查询间对齐，并加测试锁定，避免标签页计数与列表条数不一致
- 修正搜索高亮组件的 hooks 调用顺序（当前不会触发问题，但再加一个 hook 就会渲染报错），并补上两个方向的回归测试
- 清理死代码与 lint 告警：ESLint 问题 99 → 82、error 8 → 2；对两处“照 lint 建议改反而会引入 bug”的位置补了说明注释

## [5.6.0] - 2026-08-06

### 新增
- **图文混排内容完整保留**：从 Word、浏览器等处复制一段图文混排的内容（文字、图片交错），以前只能存下文字、图片全丢；现在会完整保留图文顺序与全部图片，下次粘贴出去还是原样
- **图文内容可编辑**：双击卡片打开编辑器，所见即所得修改——支持加粗、斜体、删除线、项目符号与编号列表，可删除现有图片，也可直接 Ctrl+V 粘贴或从文件插入新图片
- **图文编辑支持全屏**：内容多的时候可切到独立全屏窗口编辑，Ctrl+S 保存、Esc 关闭，有未保存修改会先提醒
- **列表里一眼认出图文条目**：卡片显示首图缩略图与「图文」徽标（带图片张数），与纯图片条目区分开

### 修复
- **删除记录时不再残留图片文件**：以前删掉图片记录后，磁盘上的图片文件一直留着不清，时间久了会白占空间；现在会自动清理（同一张图被多条记录引用时不会误删）

## [5.5.3] - 2026-08-06

### 改进
- **「海洋」主题改名为「经典白」，并恢复成纯白卡片 + 素面背景的老样子**：之前它被改成半透明玻璃效果，卡片内容发虚、整体偏灰；现在回到干净的白底，也不再跑背景动效，是最省资源的一个主题（其余五个主题的沉浸场景不受影响）
- **搜索框外观统一**：主窗口、设置、片段库、快捷粘贴等处的搜索框圆角大小不一致，现已对齐

### 修复
- **设置里的搜索框多一层边框**：看上去像框中框，已去掉
- **输入框获得焦点时出现多余边框**：主窗口搜索框点击后会冒出一圈额外的框，现已修正
- **焦点提示框遮挡旁边内容**：小尺寸按钮（如标签上的删除按钮）获得焦点时，提示框圆角取错导致圈得过大、盖住了附近的内容

## [5.5.2] - 2026-08-04

### 改进
- **大幅降低 GPU 占用**：主题皮肤场景（海洋的波光帆影、午夜的满天星、美乐蒂的爱心雨等）此前会在托盘弹窗、快捷粘贴面板这些已经看不见的窗口里继续播放，常态是三份同时在跑。现在窗口失去焦点即整层暂停，切回来自动恢复，观感无变化
- **场景动效精简**：减少了各主题的粒子数量、调低了光晕与雾气的模糊强度，画面观感基本不变，持续占用明显下降
- **历史卡片不再对背景做实时模糊**：卡片仍是半透明、皮肤场景照样透出，但取消了逐张卡片的磨砂处理，列表越长收益越明显

## [5.5.1] - 2026-08-03

### 修复
- **SQL IN（分隔值）转换丢掉首尾两个 ID**：形如 `[id1, id2, …]` 的横排列表，外层方括号没被剥掉，首尾两个值被污染。生成的语句语法合法、不报错，但那两条记录永远查不中
- **全屏预览无法选中文字**：Markdown、CSV 表格、JSON 表格、日志四个预览区既选不中也复制不了，现已放开，且复制内容不含界面上的行号与类型标记。JSON 结构树保持原样——它已有「复制值 / 复制路径」按钮，拖选只会得到残缺内容
- 日志预览复制出来时，时间、级别与消息之间没有分隔符（它们的间距来自界面排版而非文本本身）。需要原始格式请切「编辑源码」视图

## [5.5.0] - 2026-08-01

### 新增
- **变换枢纽智能推荐**：按相关度分「推荐」与「其他工具」两区，日常打开只看到 2~5 个高相关变换，不再一屏平铺
- **SQL IN 反向拆解**：从 `WHERE id IN ('a','b','c')` 提取值列表，可输出为每行一个、JSON 数组或逗号分隔
- **表格转 INSERT 语句**：支持 MySQL 边框表格与 Tab 分隔表格，自动区分数字、字符串与空值
- **数值工具箱**：进制转换、字节换算、千分位格式化、时间戳解读（含本地时间与时区）
- **美乐蒂沉浸皮肤**：粉色主题配套的图标、立绘、空状态与场景氛围

### 改进
- 变换枢纽的识别更快更准：内容只分析一遍供所有工具复用，10KB 文本分析在 5 毫秒以内
- 精简了 6 个极低频变换项，减少枢纽里的噪声
- 竖列数据识别放宽：支持 Excel 多列粘贴取首列、允许含单个空格的短值（如 "New York"）
- Base64 识别放宽：支持 URL 安全字符集、自动补齐、减少短字符串误判
- SQL IN 识别放宽：不再要求内容是标准 JSON，也能从对象里的数组字段提取

### 修复
- 粘贴到了错误的窗口：焦点离开后记录的仍是上一个目标窗口

## [5.4.5] - 2026-07-29

### 修复
- **快捷粘贴面板（Alt+V）的卡片被压成一条缝**：高度只剩十几像素、文字被裁切且无法滚动
- **快捷粘贴面板的预览文字上下各被裁掉半行**：现从第一行开始显示，末行加省略号
- **在快捷粘贴面板里删除记录后主窗口不刷新**：列表与侧边栏计数一直是旧的。此修复覆盖所有「从其它窗口删记录」的场景，不只快捷粘贴
- 快捷粘贴面板反复唤出会重复加载数据与缩略图
- 快捷粘贴面板出错时不再无迹可查：失败一定写入日志。这个面板由热键唤出，出问题时你不会去开开发者工具，而粘贴失败时它已因失焦自动隐藏，提示本身可能根本看不到

### 改进
- **快捷粘贴面板的「清除」改为两步确认**：这里的删除无法撤销，而按钮就紧靠搜索框。首次点击变为带危险色的「确认清除 N 条？」，4 秒无操作自动复位
- 已知限制：粘贴失败的提示仍可能看不到——面板失焦即被系统隐藏，要让提示可见就得抢回焦点，而那会打断你正在粘贴的目标窗口，比看不到提示更糟。目前保证失败一定入日志

## [5.4.4] - 2026-07-29

### 修复
- **导入时丢失标签**：帮助页教的迁移方式（导出 JSON → 新电脑导入）必然丢掉所有标签。现按标签名匹配本机已有标签，同名则复用，且不覆盖你在本机调过的颜色
- **导入后部分记录「计入总数却在任何分组下都看不到」**：源电脑的分组在本机不存在时，这些记录现落入「未分组」，可见也可整理

### 改进
- **缩短历史保留天数时增加确认**：先算出会删掉多少条再问你。只在变严格时拦，改长或关闭不打扰
- **删除分组增加确认**，并告知影响面：该分组下有多少条记录、这些记录不会被删除，只会变为未分组

## [5.4.3] - 2026-07-29

### 改进
- **无障碍：全局文字与主色的对比度提升至 WCAG AA 标准**（实测数据由脚本按 WCAG 相对亮度公式计算，非目测）：
  - 实心主按钮新增专用底色：原来白字压在主色上，7 套主题全部不达标（最差 2.28:1），现均为 4.5:1 以上。按钮文字仍是白色，只把底色调深
  - 主色浅底上的文字与图标：浅色三套主题原仅 3.0~3.6:1，现 4.5:1 以上；深色四套本已达标，视觉无变化
  - 卡片时间戳、设置项说明等正文级灰字提亮：最差背景下原仅 2.3:1，现 4.5:1 以上
  - 侧边栏分区标题（原最差 1.8:1）、三级文字层级（修复两套主题下层级塌陷）、绿/橙/红语义色配对同步修正
- 提示浮窗的进度条、图标色块与加载动画改为跟随主题配色，不再在 7 套主题下固定一个调子

### 修复
- **删除类按钮在深色主题下 hover 反而变暗**，方向与全应用相反，现统一为变亮
- 关于页卡片 hover 抬升时缺少阴影
- 主色描边与警告色在部分主题下不跟随主题配色

## [5.4.2] - 2026-07-29

### 修复
- 修复综合体检报告中的 P1 类数据安全与静默失败问题：
  - **事务安全**：data_store 下 12 处手写 `BEGIN;`/`COMMIT;` 事务全部改为 RAII 的 `unchecked_transaction()`。原实现在 COMMIT 自身失败时不会 ROLLBACK，会把事务永久挂在共享连接上，导致后续所有写入静默加入悬空事务并在退出时全部丢失，且重启前无法恢复
  - **长度校验**：分组/标签名、片段标题与内容补上后端长度校验（按字符数而非字节数，不误判中文），前端「添加到片段库」同步加限长保护，修复超大粘贴写入片段库后永久冻结 UI 的问题
  - **编辑器跨条目误关**：`closeEditor` 增加条目身份校验，修复「编辑 A 保存慢 → 弃改改编 B → A 的保存回调把 B 的编辑器突然关掉、B 的内容无声丢失」
  - **静默失败补 toast**：分组五个操作（含两处返回 false 仍弹成功的假成功）、片段保存/删除/导出、清理过期记录、LAN「发送测试消息」与设备刷新、置顶（右键菜单 + 悬停浮层 + 内联按钮共三处）/存片段/确认与移除自动标签、打开链接与文件、创建重名标签等
  - **片段库脏检查与防重**：遮罩点击和 X 按钮不再绕过未保存确认；保存加 in-flight 防护，快速双击不再产生重复片段；批量存片段部分失败后重试只处理失败项，不再重复存入已成功的
  - **监听器泄漏与错误兜底**：修复 TrayPopup 的 async setup + 同步 cleanup 监听器泄漏、BackToTop 的 rAF 递归轮询泄漏；托盘弹窗窗口补上 ErrorBoundary 与全局 error/unhandledrejection 兜底，不再因渲染抛错变成永久空白弹窗
  - **Rules of Hooks 违规**：SourceBadge 的提前 return 移到所有 hook 调用之后，修复 source 在空/非空间切换（异步回填、虚拟列表复用）时抛错崩掉子树
  - **标签搜索框误建标签**：搜索框的 Enter 不再误读另一个输入框的 state 而静默创建用户并不想建的标签
  - **来源识别误匹配**（上两轮遗留）：“Code” 这类过宽短别名改走精确匹配，Xcode / Codecademy / CodePen / QR Code Generator 不再被错标成 VS Code

## [5.4.1] - 2026-07-29

### 修复
- 修复 2026-07-28 综合体检报告中的 P0 类功能性破损（共 7 项）：
  - 20 处边框在全部主题下静默消失
  - 全屏编辑器、托盘弹窗等独立窗口里，提示与确认框的遮罩层级失效
  - 全应用键盘 Tab 导航几乎看不到焦点指示
  - 深色主题下三处白字白底：设置面板分段按钮提示、OCR 全文面板、图片裁剪还原按钮
  - 清除快捷键后「依次粘贴 / 收集模式开关 / 粘贴最近收集」三个热键完全失效，且这些默认组合被误报为已占用
  - 手动建立与系统同名的标签，会永久废掉对应的 AI 自动分类类别
  - 空列表的引导文案指向了不存在的按钮

## [5.4.0] - 2026-07-28

### 新增
- 文档处理工具组：编码检测与批量转码（GBK/Big5/Shift_JIS 等自动识别 → UTF-8）、配置格式互转（Properties↔YAML↔JSON）、文件级批量查找替换、历史记录导出 Excel/CSV/JSON
- 配置语义对比：跨格式 key-value 差异高亮（properties vs YAML vs JSON），自动识别格式，表格展示新增/删除/修改/相同
- 编解码工具组：Base64 / URL / Unicode / HTML 实体编解码、JWT 解析、时间戳与日期互转，共 11 个变换，按内容类型自动匹配推荐
- SQL 工具族：SQL 格式化（关键字大写 + 子句换行 + 缩进对齐）、SQL 压缩、关键字大写
- 日志统计：级别分布柱状图 + 时间范围 + 高频错误 Top5 摘要，一键提取 ERROR/FATAL 错误行（复用分类引擎同源解析）
- 片段变量：文本模板支持 {{date}} {{time}} {{datetime}} {{clipboard}} {{uuid}} 等动态变量，复制时自动解析插入
- 变换枢纽扩容：注册表现有 41 个变换，覆盖文本 / 编解码 / SQL / JSON / 日志 / 配置六大类

### 改进
- 帮助页重构为功能全景导览：快速上手 3 步卡片 + 10 张功能卡片网格（含操作路径）+ 快捷键速查 / 常见问题折叠区，去除从未实现的无效热键条目，搜索覆盖全部内容
- 顶栏工具箱分组面板：片段库 / 内容提取 / 编码转换 / 批量替换 / 配置对比按"内容 / 文本处理"分组收纳，功能图标 currentColor 化跟随主题与激活态
- 正则替换规则改用 SQLite 持久化（regex_rules 表），内存缓存同步读 + 异步写入，调用方零改动，首次运行自动从 localStorage 迁移
- 更新说明弹框改版 + Markdown 渲染行号 + 全屏编辑器增强
- README 全面同步：版本徽章、开发者工具箱段落、技术栈新增依赖、项目结构修正

### 修复
- 更新下载进度条三处问题：多源 failover（Gitee→GitHub）时进度归零、高频进度事件导致进度条抖动（改 requestAnimationFrame 节流 + 0.15s linear 过渡）、不确定态（服务器无 Content-Length）仅显示转圈动画（改显示已下载 MB + 速率）
- 卡片虚拟列表滚动告警：CardList 关闭 useFlushSync，消除 TanStack/virtual#1094 与 Lenis 平滑滚动组合产生的 dev 警告

## [5.3.2] - 2026-07-27

### 修复
- 全屏编辑器主题样式丢失（Markdown / JSON / HTML / CSV / 日志 / 代码全类型）：独立窗口入口 editor-main.tsx 缺失全局样式导入，[data-theme] 主题变量无定义，预览区回退白底黑字、h1 渐变标题透明、代码块褪色，与主窗口弹框预览不一致；已补齐 theme / buttons / dialog / code-theme 四份样式导入
- 托盘右键弹窗同类问题：popup-main.tsx 同样缺失主题样式导入（白底黑字、渐变与边框失效），且主题硬编码不跟随用户设置；已补齐样式并在启动时读取用户实际主题
- 版本徽章（VersionBadge）样式从主窗口专属 app.css 迁入 buttons.css，全屏编辑器与托盘弹窗等独立窗口均可复用

## [5.3.1] - 2026-07-27

### 改进
- 更新弹框改版：更新日志改为时间线式排布（分类条目沿竖轴展开），顶部分类筛选 chips（全部 / 新增 / 改进 / 修复）一键过滤，渐变洗色头部 + 版本摘要引语，长日志滚动定位更清晰

### 修复
- 全屏编辑器滚动条在深色主题下不可见（滑块色与暗背景融合）：改为 8px 主题自适应常显滚动条，编辑区暗底用浅色滑块、亮色主题自动转深色，预览及 JSON / CSV / 日志滚动区按主题取色

## [5.3.0] - 2026-07-26

### 新增
- 事件驱动剪贴板监听（Windows）：AddClipboardFormatListener + WM_CLIPBOARDUPDATE 原生通知，两级管线（消息线程 50ms 防抖快速捕获、工作线程承担图标提取/过滤/智能合并等重处理），另有按序列号兜底补读的定时器，消除旧 400ms 轮询"快速连续复制时中间内容永久丢失"的采样盲区
- 栈模式队列面板：横幅内嵌状态行 + 队列 chips + 进度行三层结构，"逐条粘贴"升为主按钮并带热键提示，已粘贴项折叠为内容预览 chips

### 改进
- 动画体系大修（16 项）：卡片悬停抬升 / 按压反馈改用 framer 手势通道（修复内联 transform 覆盖样式表导致的悬停失效）、筛选切换整行交叉淡入、列表 ↔ 空状态过渡、Toast 移除后弹簧补位、标签芯片统一弹入弹出入口、全屏编辑器视图切换动画、设置分区逐级入场、右键子菜单退场淡出、.md 关联横幅高度塌缩退场
- 支持系统"减少动态效果"设置：四通道联动降级（MotionConfig + 全局 CSS 媒体查询 + 对话框动画门控 + 编辑器窗口 no-anim），Lenis 平滑滚动同步禁用
- 图标提取移至工作线程：捕获阶段只记录进程路径，不再阻塞消息循环，同时规避 hwnd 在异步阶段失效的问题
- 设置面板页脚改为紧凑两行布局；栈模式热键提示动态读取设置，修改后即时生效

### 修复
- 栈模式卡片底色（.cardInStack）硬编码浅色导致与暗色主题不协调
- 移除从未被引用的 AboutDialog / HelpDialog 组件（内容已整合进设置面板）

## [5.2.0] - 2026-07-26

### 新增
- 全屏编辑器泛化：通用外壳 + 内容类型注册表架构，文本 / JSON / HTML / CSV / 日志 / 代码等多类内容均可独立窗口全屏编辑
- 日志全屏预览：徽章流 + 搜索 + WARN/ERROR 着色，与后端分类引擎同源解析
- JSON 编辑升级：语法检查（错误徽章 + 一键跳转错误行）+ 树形预览浏览器
- .md 文件关联：设置页开关注册 PastePanda 为 .md 打开方式，横幅引导设为系统默认，双击 .md 文件直接全屏编辑
- 变换枢纽：右键"变换为…"打开卡片流，内置 19 项通用文本变换与 SQL IN 子句 / JSON 插入等场景变换，支持动态选项
- 深度清理：按时间范围 / 内容类型 / 来源应用自由组合条件，实时匹配计数，可先预览再删除，自动跳过置顶记录，支持 Ctrl+Z 撤销
- 设置页数据仪表盘：7 日趋势、24 小时分布、来源 TOP 5、今昨对比一目了然
- 链接 / 数字 / 路径专用编辑器：URL 结构拆解与一键动作，时间戳 / 进制 / 字节单位换算工具箱，路径形状拆解
- 全屏编辑器保存可写入剪贴板历史，输入停顿自动保存（均可在设置中开关）

### 改进
- 编辑器提速：懒加载 + Suspense + 依赖预构建，CodeMirror 面板常驻、切换即时
- UI 精致化：右键菜单与 Toast 玻璃态、粘贴栈横幅主题自适应、空状态动画、Sidebar 交互、TopBar 来源筛选下拉
- 更新日志改为卡片流展示，分类图标 + 色块，当前版本高亮
- 右键菜单重整：新增"粘贴并变换"子菜单，二级菜单超屏自动翻转
- 自动过期清理移至后端调度器：关窗驻留托盘仍按时执行，不再占用撤销栈
- 敏感内容防护改为设置开关（默认关闭），把选择权交给用户
- 过期记录计数在清理操作后自动刷新

### 修复
- 弹窗打开时鼠标滚轮穿透滚动底层列表
- 卡片预览气泡位置偏移与动画漂移
- 来源筛选下拉宽度被均分拉伸

## [5.1.2] - 2026-07-24

### 新增
- 专用编辑器：颜色值（色块预览 + HEX/RGB/HSL 一键复制）、CSV 表格（表格/编辑双视图 + 导出 Markdown/JSON）、密钥/Token（自动脱敏 + 类型识别）
- 图片格式转换与压缩：支持 PNG/JPG/WebP 切换，可调质量，实时估算文件大小
- 图片裁剪：自由选区 + 三分线辅助，确认后可还原原图
- 多文件卡片：同时查看多个文件，汇总信息一目了然，可批量打开文件夹或复制路径
- 文件快速预览：图片缩略图、文本文件前 20 行带行号，无需打开外部程序

### 改进
- 密钥类内容在卡片列表自动脱敏显示，复制时仍为真实值
- 文件详情弹窗自适应宽度，多文件列表支持点击切换预览

### 修复
- 旧版单文件路径数据兼容修复

## [5.1.1] - 2026-07-23

### 新增
- 版本更新弹框：检测到新版本时自动弹出，展示分类更新日志，支持跳过版本和一键下载
- 更新日志浏览器：设置 → 关于页可查看版本历史，当前版本高亮
- 关于页未读红点：有新版本或未查看日志时显示红点提醒
- 下载进度环与速率显示：实时展示下载进度和速度

### 改进
- 更新下载支持多源加速（国内 Gitee 镜像），失败自动切换
- 支持跳过指定版本，不再重复提醒
- 主题新增发光效果变量，视觉更精致

## [5.1.0] - 2026-07-22

### 改进
- 内容类型识别更准确，统一由后端分类引擎处理
- 历史清理改为定时周期执行，时区处理更可靠
- 内部架构优化，模块拆分更易维护

## [5.0.148] - 2026-07-21

### 新增
- 粘贴栈模式：Ctrl+Shift+K 开启，Ctrl+C 逐条收集，Ctrl+Shift+P 逐条粘贴或全部粘贴，快捷键可自定义
- 栈模式横幅：显示收集/粘贴进度，支持全部粘贴和中止操作
- 入栈卡片标记：橙色边框 + 序号角标，栈顶脉冲高亮

### 改进
- 用户体验专项优化 58 项：删除操作统一为即时删除 + 撤销提示、Esc 关闭按层级处理、启动不再抢焦点、焦点陷阱与无障碍支持、热键冲突检测与回滚、敏感内容防护（密钥/凭证模式不记录）
- 粘贴成功/失败分别提示，错误信息更具体
- 收藏/置顶操作即时保存，重启不再丢失
- 剪贴板捕获改为静默记录，不再每次弹 toast
- 片段库：使用次数真实显示、空名称禁止保存、未保存弹确认
- 热键录制器：支持清除、Esc 取消、校验提示常驻

### 安全
- 局域网同步改为 AES-256-GCM 加密 + 重放防护
- 资源限制：图片解码尺寸/体积上限、正则执行超时保护、导入条数上限

### 修复
- 修复快速连按导致重复粘贴或跳过条目
- 修复粘贴抑制期间丢失正常剪贴板变化
- 修复筛选缓存失效、置顶加载更多竞态等问题
- 修复图片预览快速切换竞态、多文件路径变换损坏
- 修复虚拟列表滚动流畅度与缩略图按需加载
- 修复前台窗口校验失败可能粘贴到错误窗口

## [5.0.147] - 2026-07-19

### 新增
- 正则过滤/替换：右键菜单「粘贴并变换 → 正则替换」，预设 8 条规则（去空行/去空格/URL 解码/手机号脱敏/身份证脱敏等），支持自定义增删改
- 正则预览对话框：左右分栏实时对比原文与替换结果，可临时调试正则

## [5.0.146] - 2026-07-19

### 新增
- 文本对比（Diff）：Ctrl 多选 2 条文本记录，点击「对比」打开全屏对比视图
- 支持按行/按词两种对比模式、忽略空白、同步滚动
- 差异跳转导航（上一处/下一处），可复制旧文本或新文本

## [5.0.145] - 2026-07-19

### 新增
- 二维码生成：右键菜单生成 QR 码，支持 URL/文本，可复制图片或保存 PNG
- HTML 转纯文本：右键菜单「粘贴并变换 → 剥离 HTML 标签」

## [5.0.144] - 2026-07-19

### 新增
- Markdown 实时预览：编辑/预览切换，支持 GFM 全语法（标题/列表/代码块/表格/任务列表/图片）
- 代码块自动语法高亮
- 预览模式支持「复制为 HTML」「复制为纯文本」
- 卡片 hover 弹窗支持 Markdown 渲染，蓝色「MD」徽标标识

## [5.0.143] - 2026-07-19

### 新增
- 颜色值预览：剪贴板内容为颜色值时，卡片直接显示实时色块 + 格式标识
- 颜色格式互转：右键菜单「复制为 HEX/RGB/HSL」，支持透明度保留

## [5.0.142] - 2026-07-19

### 安全
- 修复文件操作路径校验缺失导致的任意文件读写风险
- 修复系统打开命令注入风险
- 修复局域网同步无鉴权问题，新增配对密钥签名校验

### 修复
- 修复数据库迁移失败静默吞错、搜索关键词未转义
- 修复粘贴抑制期间丢失剪贴板变化、分组状态不同步
- 修复筛选缓存失效、置顶并发竞态
- 修复图片预览竞态覆盖、多文件路径变换损坏
- 修复内容分类误判（短 JSON、命令行、驼峰标识符）
- 修复极端宽高比缩略图崩溃、高 DPI 缩放图标尺寸错误
- 修复热键冲突无提示、托盘右键连点阻塞
- 修复长文本截断切断 emoji 等多字节字符

## [5.0.141] - 2026-07-07

### 修复
- 修复依次粘贴越界与测试稳定性问题

## [5.0.140] - 2026-07-07

### 修复
- 修复依次粘贴在微信/企业微信等应用中失效
- 修复多条粘贴时目标窗口错误（加 2 秒 TTL 锁定）

## [5.0.126] - 2026-07-06

### 改进
- 侧边栏宽度收窄（220px → 180px），内容区更宽敞
- 侧边栏开关动画改为纯 CSS，窗口不再跟着变宽

### 修复
- 修复窗口状态恢复（插件确保先恢复后显示）
- 修复侧边栏开关时窗口宽度漂移

## [5.0.118] - 2026-07-05

### 新增
- 侧边栏改版：macOS Finder 风格，独立背景色，来源分组带颜色标识

### 修复
- 依次粘贴悬浮按钮移至卡片面板内，侧边栏打开时不再遮挡

## [5.0.117] - 2026-07-05

### 修复
- 修复自动更新检查失败（CSP 域名缺失）
- 修复回到顶部按钮点击失效

## [5.0.113] - 2026-07-04

### 改进
- 设置对话框架构优化，组件化拆分

## [5.0.112] - 2026-07-04

### 改进
- 引入平滑滚动库，列表滚动体验更流畅
- 移除快捷键悬浮按钮，布局更简洁
- 分类 Tab 计数改为后端查询 + 缓存，数据更准确

## [5.0.111] - 2026-07-03

### 改进
- 时间轴 Mini 模式优化，节点间距自动对齐
- 设置页滚动时自动隐藏 Tab Bar，切换带动画
- 样式模块化重构，从全局 CSS 拆分为组件级模块
