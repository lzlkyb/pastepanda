# 知识库同步：审查与规划（2026-09-07）

对 M6 同步（`src-tauri/src/sync/`）做了一次整体审查 + 外部调研。
本文只管**知识库同步**（iroh），不管局域网剪贴板同步（组播+AES，另一套栈）。

> 已落地的不在本文讨论范围：双边配对（`sync::join`）、退避与休眠、
> 在线状态判据（`src/lib/kbOnline.ts`）、脏了才拨 + 写入即唤醒。见 commit `a7d0eff`。

---

## 一、先记下不该动的部分

改任何一条之前先读这四条，它们是同类项目里最容易做错而这里做对了的：

1. **游标只能推到共同高水位 H**（`session.rs` 模块注释的推导）。
   推成本机最大值 → 回声 → `both_changed` 恒真 → 每轮再生一批冲突副本；
   直接推两边最大值 → 会话期间刚改的那篇被**永久跳过**（静默丢数据）。
2. **部分失败会按住游标**（`missing_files` / `import_failed`）。
3. **身份一律从已认证连接取**，hello 不带 `node_id`、公告不带设备名。
4. **内容相同即跳过**（回声拦截）。

---

## 二、十字总览

| | 问题 | 严重度 | 方案 | 估工 |
|---|---|---|---|---|
| W1 | 笔记里的**图片根本不同步** | 🔴 静默功能缺口 | A：同步时双向改写引用 | 中 |
| W2 | 一旦分叉**永不自愈** | 🔴 架构缺口 | b：分桶摘要，只重对账分叉的桶 | 小中 |
| W3 | 墓碑无限增长 | ⚠ 长期退化 | 双条件：所有游标越过 **且** 超龄 | 小 |
| W4 | 合并粒度是整篇 | ⚠ 体验 | **先做并排 diff**，真三方合并降级为独立提案 | 小 / 大 |
| W5 | 临时 vault 目录的磁盘往返 | ✅ **重估完毕，不动** | 并发闸把峰值压回 2，原前提重新成立 | — |
| W6 | 全网状拓扑 | ✅ **止血三条已做** | 阈值已触发 → 全局并发闸 / 抖动按比例 / 唤醒合并，均已落地（拓扑本身不动） | 小 |

顺序按 W1 → W2 → W3 → W4。W1/W2 互不依赖；W3 依赖 devices 表（已有）；
W4 依赖一个尚不存在的存储（见下）。

---

## W1　图片不同步

### 现状

```
落盘：  <app_data>/images/<md5>.<ext>          ← 文件名就是内容 hash
正文：  file:///C:/Users/19145/AppData/Roaming/com.pastepanda.app/images/<md5>.png
渲染：  convertFileSrc() → asset://
```

`engine.rs` 里搜不到任何附件处理——同步清单只搬 markdown。

🔴 **引用本身是机器特定的**（结尾绝对路径里带 Windows 用户名）。
所以光搬字节没用，必须同时解决「搬字节」与「引用便携化」。

### 方案

**A（采用）同步时双向改写引用，图片随清单走**

- 导出增量：`file:///<绝对路径>/images/<hash>.<ext>` → 相对的 `images/<hash>.<ext>`，
  用到的图片复制进临时 vault 的 `images/` 子目录
- 导入：图片落到本机 images 目录（**同 hash 即同内容，已存在就跳过**），引用改写回本机绝对路径
- **库里的存储格式一行不改** → 零数据迁移

**B（否决）把库里就改成便携形式**（`pp-image://<hash>.<ext>`）。
长期更干净，但要迁移全库历史正文、且触及采集/编辑/渲染多条路径。
迁移出错 = 所有图全挂。风险与收益不成比例。

**C（后置）只搬引用，图片按需向对端拉取**（走 iroh blob）。
不改会话形状、大库不会一次搬几百 MB，但要新协议通道且**对端离线就断图**。
A 做完后 C 可以作为纯优化叠上去，两者不冲突。

### 实现要点

- 正文有**两种形态**：富文本 HTML（`src/lib/richContent.ts`）与 markdown。
  改写函数要收口成一份（规则 #11），两个方向：`to_portable_refs` / `to_local_refs`。
- 🔴 **只改写精确匹配 images 目录模式的引用**，其余（http(s)、长得像的本地路径）
  一律不动。改写函数弄坏正文是这一项最大的风险面。
- 单张上限 **10MB**（已拍板，见§四），超过就跳过并**进报告**（不静默，规则 #15.3）。
- 清单要列出图片，否则无法复用现有的「清单说有、文件没到 → 按住游标」机制。

### 落地记录（2026-09-07）

已完成：`sync/attach.rs`（新）+ `engine.rs` 两侧接线 + 18 条测试（含 2 条引擎层端到端）。

**与上面方案的三处偏离，都有理由：**

1. **便携形式用 `pp-asset:<hash>.<ext>`，不用相对路径 `images/<hash>.<ext>`**。
   笔记在 vault 里按文件夹散在不同深度，相对路径得按深度拼 `../`，多一层少一层都是错；
   而一个自定 scheme 与位置无关。
2. **附件目录叫 `.pp-sync-assets`（带前导点）**。`note_vault.rs` 的 `collect_md`
   跳过所有 `.` 开头的目录；不带点的话 `images/` 会被当成**一个笔记文件夹**，
   对端库里凭空多出一个叫 images 的文件夹。已单独钉了一条测试。
3. **新增 `ExportReport.assets_skipped` 并上到界面**。原方案只说「进报告」，
   但报告链只有 `ApplyReport`（接收侧）。而附件没搬成是**发送侧**才知道的事：
   对端只会看到一张断图且无从分辨，两边界面都会显示「同步完成」。

**一条顺序不变式（已用测试钉住）：**
附件落盘 + 引用改写必须在 `apply_delta` 的逐篇循环**之前**跑完，不能拆进循环。
循环里那条回声拦截（`identical`）是拿文件文本与本地笔记比内容的，
而本地存的是绝对路径；文件里还留着 `pp-asset:` 就**永远比不相等** →
回声那一批每篇都满足「两边都改过」→ **每轮再生一批冲突副本**。

**测试抓出的两个真 bug（同一个根因）：**
引用正则里的「盘符」`[A-Za-z]:[/\\]` 会拿 **scheme 的最后一个字母**当盘符：
`https://…` 里的 `s:/` → 远程图被错改写；`file:///C:/…` 里的 `e:/` →
从第 4 个字符起匹配、改写后留下 `filpp-asset:…`，回程变成 `filfile:///…`。
后者恰好打破上面那条不变式。修法：盘符前加 `\b`。

**还没做：**双机实测（本地只能验到引擎层）。

### 验收

- `engine` 层回环测试（两个 `DataStore` + 临时目录，不联网）：
  带图笔记 → 导出 → 导入 → 图字节到位且引用能解到本机路径
- 对抗测试：http 图片、奇怪但不属于 images 目录的本地路径、同一张图被多篇引用
- 同 hash 已存在时不重写（去重）

---

## W2　分叉不可自愈

### 现状

反熵完全靠单调游标。两边内容若因任何原因分叉而游标都已推过，
**没有任何机制能发现「我们不一致」**。协议里无校验和、无 Merkle 树。

现有设计能保证「正常路径收敛」，**不能保证「异常之后收敛」**。

### 方案

**a** 会话末互报全库摘要（所有 `(id, updated_ms)` 排序后哈希），不一致就把游标退回 0 全量对账。
简单，但**一次分叉就全量重传**。

**b（采用）分桶摘要**：按 `id` 前缀分 16 个桶，各报一个哈希；
只把**分叉的桶**退回重对账。是 a 的小幅扩展（约 60 行），避开了全量重传。

**c（不采用）** 上 RBSR / Merkle——即 iroh-docs 路线，见第三节。

### 🔴 关键约束

**摘要只在心跳会话做，不在每次改动触发的会话做。**
摘要要扫一遍 `(id, updated_ms)`（而且没有 `updated_ms` 索引），
每次都算会把刚做完的「脏了才拨」的成本优势吃掉一部分。

另：游标退回后依赖「内容相同即跳过」这条已有拦截，否则全量重对账会生一堆冲突副本。
那条拦截已经在位（`engine.rs` 的 `identical`），但验收时必须专门钉一条。

### 落地记录（2026-09-07）

新增 `sync/digest.rs` + `note.rs` 两个分桶筛选查询 + `session.rs` 一个摘要帧，
11 条测试。实际比估的「约 60 行」大，多出来的都花在下面这条上。

**🔴 方案里没看到的一条：摘要不能只一边做。**

分叉集是两份摘要的纯函数，所以**两边都拿到对方摘要**时两边会算出同一个结果，
不需要再多一个“请求重发”帧。但反过来，若只有一边拿到，修复就是**单向**的，
而单向修复**不收敛**：A 把桶 k 全发给 B，B 按后写胜并入；但若 B 那边有更新的版本，
A 学不到，下一轮依旧分叉。

而 hello 是「先写再读」，接入侧读到拨号方的 hello 时自己那份已经发出去了，
没法事后把摘要塞进 hello。所以摘要单独一个帧，hello 里只带两个标志位。

**三处取舍：**

1. **协议版本不升，改用能力位 `digest_capable`（`#[serde(default)]`）**。
   升 `PROTO_V` 会让一台升了、一台没升的用户直接同不了；一个 bool 就能退化兼容。
   🔴 这个位必须看：不看就直接发摘要帧的话，本机会写出一个旧版永远不会读的帧，
   然后自己卡在读一个永远不来的帧上——**挂死**。已用一条反序列化测试钉住默认值。
2. **只要一边 `want_digest` 就交换**。两边各自按自己的心跳节拍走，
   要求同时想的话几乎永远对不上，摘要就埋成了死代码。
   给 `true` 的四个时机：服务刚起的第一拨（上次可能崩在会话中间）、心跳到点、
   从休眠里醒来、用户点「立即同步」。脏了而醒与退避重试一律 `false`。
3. **分桶口径在 Rust 与 SQL 里各写一遍，两个定义放同一个模块**。
   两处算得不一样不会报错：摘要按一种口径分桶、重发按另一种口径筛，
   于是分叉的桶永远补不齐，而每个心跳会话都重发一遍。测试不另拼 SQL，
   而是直接问生产路径（`note_changed_since_or_buckets(i64::MAX, &[k])`）。

**其它几个已确定的点：**

- 分桶用**首字符码点取模**，不用「首个 hex 字符」：uuid v4 首位确实是 hex，
  但 `note_create_keeping_id` 允许 vault 导入带任意 id 进来。
- 摘要用 FNV-1a 而不是 sha2：对端已经是 ed25519 认过的，而就算报假摘要，
  后果也只是「多重发几个桶」，既不丢数据也不泄密。
- 墓碑**不进摘要**，但会跟着桶重发。「A 有活的 X、B 已删 X」这种真分叉
  靠活笔记就能发现；而两边都没活笔记的陈旧墓碑差异是无害的，
  进摘要只会把桶天天弄成对不上。
- 验收那条已钉：全桶重对账 20 篇 → `conflicts == 0`、`identical == 20`、
  `created + updated == 0`、且游标不动。

**还没做：**双机实测（摘要帧真的在线上跑起来、与旧版对端真的不挂死）。

---

## W3　墓碑无限增长

### 现状

`note_tombstones` 只在**还原**笔记时删（`WHERE note_id = ?1 AND purged = 0`），
没有基于时间的清理。每一条曾被删除的笔记永久留一行，
并且对游标落后的对端每次都要重发一遍。

### 方案：双条件，缺一不可

```
可删 = 所有已配对设备的 sync_cursor_ms 都 > 该墓碑的 local_ms
        且 墓碑年龄 > 安全期（建议：回收站保留期 + 30 天）
```

- 只用游标条件：某台设备长期不开机 → 墓碑永远删不掉（卡死）
- 只用年龄条件：对端离线超过安全期后回来 → **删除丢失 → 对端那边笔记复活**

### 🔴 这是六项里最危险的一项

删早了的后果是**已删的笔记复活**，而用户会以为是同步把垃圾又搬回来了。
所以：宁可墓碑多留一年，也不能提前删。安全期宁大勿小。

被「忘记」的设备不计入游标条件（它已不在 devices 表）——这正好是想要的。

### 落地记录（2026-09-07）

`note.rs` 两个函数 + 接到现有的每小时 `auto_cleanup` 循环上，11 条测试。

**两个函数而不是一个，是为了可测：**
`tombstone_gc(min_cursor, cutoff)` 不碰墙上时针，两个条件能各自钉死；
`tombstone_purge_expired_at(safe_days, now)` 把「现在」也开成入参。
不开的话墓碑刚生成时 `local_ms ≈ 现在`，永远跑不到「年龄够了」那一支，
于是「取最小游标」那条真正拉着安全底线的逻辑一行也盖不到。
已验过它是真闸门：`.min()` 改成 `.max()` 后正好那一条挂、其它 10 条不变。

**`note_trash_days == 0` 时不跟着关。**
安全期退回默认 30 天再加 30 天。理由就写在 `auto_cleanup.rs` 开头：
两个开关静默联动、而用户无从得知，是那个模块犯过的错。
「不自动销毁笔记」是关于笔记的，与墓碑这份同步记账无关。
默认配置下安全期 = 60 天。

**三条“回收不得改变任何东西”的不变式（都已钉）：**

1. 回收后任一对端能拿到的墓碑集合**逐条不变**。不成立就意味着有对端
   丢了一条删除 → 那边笔记复活。
2. 回收不改变脏检查（方案 A 靠它决定要不要拨号）。
3. 回收不改变分桶摘要（W2）。墓碑本来就不在摘要里，所以两台机器
   回收时间不同也不会凭空报分叉。❗ 哪天把墓碑加进摘要，这条会 red——
   那时得先想清楚：A 已回收、B 还没回收时那个桶会**永远**对不上。

**没做且有意不做的：**

- 不加界面提示。回收是无需用户动手的后台家务，`log::info!` 够了。
  一台设备永不开机会把回收卡死，但墓碑一条约 100 字节，
  一万条删除也就 1MB —— 不值得为它造一个告警。
- 不单开开关。没人会去关它，而 `safe_days <= 0` 已经是一个逃生口。

**残留风险（已知、靠安全期压）：**
设备 B 被「忘记」后重新配对。重配时 B 的游标归 0，而 B 本地那篇笔记从未被删；
若墓碑已在「忘记期间」被回收（因为忘记后不再计入），B 会把活的副本推回来 → **复活**。
这正是安全期宁大勿小的原因：忘记后 60 天内重配仍然安全。

---

## W4　合并粒度是整篇

### ❗ 先更正一个我之前的说法

我在口头审查时说「共同祖先我们已经有了（`note_revisions`）」——**不准确**。

核实后的实际情况：

- `snapshot_note_on` 确实在**每次** `note_update` / `note_restore` 时拍快照 ✅
- 但保留上限是 `MAX_REVISIONS`（20）份普通 + 所有锚定份
- 🔴 **快照的时间字段是 `created_at`（本地时间字串），不是 `updated_ms`（HLC 毫秒）**

游标是 HLC 毫秒，而 HLC 在时钟回拨/吸收对端下界时**有意偏离墙钟**。
所以「找出游标那一刻的版本」只能按时间近似匹配。

**而三方合并拿错 base 的后果是静默产出错内容——比现在的冲突副本严重得多。**

### 因此拆成两步

**W4a（先做，小）并排 diff 视图**
冲突副本仍然产生，但给一个「本机版 vs 对端版」的并排对照 + 一键采用。
零风险（不改合并语义），直接改善现在「两份副本扑在那里要用户自己读」的体验。

**W4b（后置，大，需单独拍方案）真三方合并**
前提是新增一份**同步基线**存储：每次成功同步后记下那一刻的内容（或其哈希）。
三个子选项待评估：
- 每篇一份全局 base（多对端时不准）
- 每篇×每对端一份 base（准，但存储 ×N）
- 双方在清单里互报 base 哈希，只在两边 base 一致时才合并（最稳，协议要改）

建议：**W1–W3 落地后再单独拍 W4b**，不在本轮堆进去。

### W4a 落地记录（2026-09-07）

设计稿：`design/知识库冲突对照-设计稿.html`。
新增 `lib/kbConflict.ts`（纯解析）+ `NoteConflictView.tsx` + `NoteConflictBanner.tsx`，
接进 `NoteDetailPane` 与 `NoteDialog`。**零后端改动、零迁移**。19 条测试。

**三个决策的最终去向（其中两个是设计稿里改过的）：**

1. **位置：换编辑区**（同 `NoteHistoryView`）。决定性理由不是美观，
   而是宿主接线现成：采用后“把新内容写回编辑器、切回编辑视图”与版本历史一模一样。
2. 🔴 **关联：解正文，不建表**（原本推荐建表，改了）。
   副本自己会同步过去，而本机表不会 → 对端打开副本时查不到原笔记，
   「在任一台处理一次、两边都干净」就没了。
3. 🔴 **断点：`@container` 按对照区自身宽度（640px）**，不是 `@media` 按窗宽（改了）。
   知识库是三段布局，900px 窗宽下详情区可能只有 400px。

**实现中测试抓出的一个 bug：**
`stripFrontmatter` 原本先判 `---` 前缀、后去开头空行。但副本里分隔符后面跟着
一个空行，传进来的是 `"\n---\ntitle: …"` —— 前缀对不上，整份 frontmatter 原样返回，
而那正是这个函数要除掉的噪声。四条测试同时红，一个根因。

**🔴 列头不写「本机版 / 对端版」：**
副本正文里那句「来自本机/对端那一份」是**建副本那台**写下的，
而副本会同步过去 —— 在另一台上读到的「本机」指的却是对方。
副本里没带 node id，无法分辨，所以列头只用在两台上都成立的说法：
「当前保留的」与「副本里那一份」。

**两条已钉的危险行为（都验过是真闸门）：**

- 「用副本那一份」必须先过确认框，取消就不能碰后端；
- 🔴 顺序必须是**先写原笔记、再删副本**。反过来的话写失败时副本已经没了，
  那份内容只剩回收站里。（把两句调反后正好那一条测试挂、其它 6 条不动。）

**有意的两条降级（规则 #15.3，不假装）：**
解不出 `原笔记 id` 那一行 → 只给「手动拼」；原笔记不在了 → 如实说
「可能已被删，也可能还没同步过来，内容就在本篇正文里」。

**只做了 副本→原笔记 一个方向的入口。**
设计稿里写的两个入口，反方向（打开原笔记时提示有副本）**后置**：
它要拿原笔记 id 去做一次**内容搜索**，而 FTS5 对 uuid 的分词行为得先验。
而真实流程（状态条 → 搜 “conflict” → 点开副本）只用得到正方向。

---

## ❗ W1–W4a → W5/W6 的交接（2026-09-07）

W1–W4a 已落地并提交（`0f07154` / `fe06803` / `9b458c3` / `9325d6a`）。
W5/W6 要动的正是它们改过的那几个函数，而下面前两条是**语义耦合**，
git 合不出来。

### ① 🔴 `peer_loop` 已经不是下文表格里那个形状了

`idle_wait` 现在返的是 `Woke` 枚举（`Stop` / `Dirty` / `Heartbeat`）而不是 `bool`；
`Wait` 的三支各自设 `want_digest`（W2）。

**`want_digest = true` 只在三个地方产生：**循环的第一拨、`Woke::Heartbeat`、
`Wait::Dormant`（另加 `sync_now` 手动同步）。

W6 要把「广播唤醒」改成「按对端唤醒」，动的就是这段。
**若重排 dormant / wake 结构而没带上这三处，分桶摘要会静默不再触发**——
而它不触发不报错，只是「分叉永远修不好」，恰好是 W2 要解决的那个静默问题原样回来。

### ② 🔴 W6 的成本表要加两项（W1/W2 之后每次会话变贵了）

下文那张表算的「N-1 份全量 delta 导出」是 **W1/W2 之前**的账：

- **W1**：导出现在还会把引用到的图片**字节**拷进临时目录（单张上限 10MB）。
  8 台时一次编辑最坏是 7 ×（文本 + 图片字节）。
- **W2**：`sync_bucket_digests()` 是一次 `notes` 全表扫（`ORDER BY id` 走主键索引，
  仍是 O(n)）。它只在 `want_digest` 的会话做，但 **W6 自己指出的「心跳惊群」
  （抖动只有 ±1.7%）恰好会让这些摘要会话聚簇** —— 8 台时可能 7 个并发全表扫。

方向上这两项都被 W6 的全局并发闸（Semaphore=2）同时压住，
所以**那个闸的价值比本文原估的更高**；反过来，不加闸而先改 W5 意义不大。

### ③ 星型拓扑那条零改动方案对 W2 有一个副作用

W2 的分叉修复要求**两边都拿到对方摘要**（单向修复不收敛，理由在 `sync/digest.rs`）。
星型下两片叶子不直连 —— 叶子 A 与叶子 B 的分叉只能经中心，要两跳两轮才收敛
（A↔C 一轮、C↔B 一轮）。不是错，但得知道它比全网状慢一轮。

### ④ 两个窗口共一个仓，git 索引得各自看一眼

本窗口两次看到别的会话把文档的**删除**暂存进了索引
（`docs/功能清单-v6.x.md`、`docs/沉浸式皮肤升级方案-v2.md`），
已 `git reset` 撤出来、没带进我的提交。那两个删除**目前还悬在工作区**（未暂存），
得有人拍是不是真要删。建议提交前一律先 `git diff --cached --name-only` 核一眼。

### ⑤ 四项都没做双机实测

W1 图片同步、W2 摘要帧（含与旧版对端不挂死）、W3 墓碑回收、W4a 对照，
**全部只验到单机层**。发版前得跑一遍。

---

## W5　临时 vault 目录——不动

N 篇变更 = N 次文件写 + N 次读 + 一份清单，`missing_files` 这个失败面就是它的产物。

但模块注释明说这是有意的取舍：「本地就能端到端测，不需要网络」。
当前规模下可测性值这个价。**改它等于拆掉现有测试的基础，不做。**

> ⚠ **2026-09-07 补：这条取舍的前提变了。**
> 「当前规模」指的是 2–3 台。W6 阈值已触发后，**N-1 个并发会话 = N-1 份磁盘往返**
> （8 台时是 7 份）。结论本身可能仍成立（可测性确实值钱），
> 但**不能照搬原理由**——得在新台数下重新算一遍。
> 注：先做 W6 的全局并发闸（Semaphore=2）可以把这个尖峰压回去，
> 所以**两者要一起看**：闸加了，W5 可能就不用动。

### ✅ 重估完毕：**仍然不动**（2026-09-07）

上面那条补充把判据写死了——「闸加了，W5 可能就不用动」。闸已经加了，所以现在算得出来：

| | 补充当时（无闸） | 现在（`MAX_CONCURRENT_SESSIONS = 2`） |
|---|---|---|
| 并发暂存目录峰值 | N-1（8 台 = 7） | **恒为 2，与台数无关** |
| 编辑突发时的会话数 | 每次写入广播一轮 | 被合并窗口压掉一大截 |

🔴 **尖峰回到了 2，而 2 正是当初「当前规模」那个前提的量级。**
也就是说：不是「结论巧合还成立」，是**那个失效的前提被闸重新建立起来了**，
所以原理由（可测性值这个价）可以照搬了。

❗ 顺带更正一处用词：每会话写的是 **delta**（`compute_delta_in(store, since, buckets)`），
不是「全量」。单次磁盘量比补充里估的小，只有首次同步是大的。

> ⚠ **重新打开的条件**（写成可判定的）：`MAX_CONCURRENT_SESSIONS` 上调到 4 以上，
> 或者双机实测发现 `%TEMP%` 磁盘吞吐成为瓶颈。否则不再看。

---

## W6　全网状拓扑——🔴 **阈值已触发（2026-09-07）**

> ⚠ **本节原标「不动，记下阈值」。用户已确认现在有 >4 台的需求，阈值到了。**
> 但原定的应对方案（“直接评估 iroh-docs”）**现在走不通**——见下一节的重新调研。
> 所以短期是**在现有全网状上加闸**，不是换协议。

N 台设备 = N(N-1)/2 对循环，各自跑心跳。

### 代码核实（三条都坐实了）

| 事实 | 出处 |
|---|---|
| **没有全局并发闸**（改前） | `coordinate.rs` 的 `slots: HashMap<peer, Slot>`，`try_hold` 是**按对端**的；全文无 `Semaphore`。✅ 2026-09-07 已加，见 `MAX_CONCURRENT_SESSIONS` |
| **写入唤醒是广播** | `service.rs` 的 `wake_all()` → `notify_waiters()` |
| **每会话一个临时目录** | `session.rs:425` `pp_session_{tag}_{uuid}`，全量导出到磁盘 |

### 代入台数

| 台数 | 全网对数 | 每台 peer_loop | **一次写入拨号数** | **并发临时目录峰值** |
|---:|---:|---:|---:|---:|
| 3（原设计点） | 3 | 2 | 2 | 2 |
| 5 | 10 | 4 | 4 | 4 |
| 6 | 15 | 5 | 5 | 5 |
| 8 | 28 | 7 | 7 | 7 |

🔴 **`service.rs:109` 那句注释在 >4 台时已经不成立了**：

```rust
/// ❗ 不按对端分开：`notify_waiters()` 把所有在长睡的都叫起来，多拨几次无害，
```

「多拨几次无害」在 2–3 台时对，**8 台时一次编辑 = 7 次拨号 + 7 份 delta 导出到磁盘**，
而且没有任何东西挡着它们同时发生。

✅ **2026-09-07 已改**：那句注释已改成说明它在 >4 台时不成立，
并指向现在真正卡量的两处（全局闸 + 合并窗口）。**广播本身没改**：
改成按对端要多一张表与一套生命周期，仍然不值。

❗ 顺带更正上面一处用词：每会话导出的是 **delta（`compute_delta_in(store, since, buckets)`）**，
不是全量。磁盘代价比原文估的小，但**并发个数**这个问题不变。

### 本文原先漏掉的一条：心跳惊群

（以下是**改前**的状态，✅ 2026-09-07 已修）

`HEARTBEAT_SECS = 600` 但 `JITTER_SECS = 10`，**抖动只有周期的 ±1.7%**。
台数一多，心跳会聚簇成惊群——这是台数越多越明显的那类问题。

### 🟢 先看这条：星型拓扑今天就能用，零代码改动

`service.rs:814` 是 `for d in &known { start_peer(&ctx, &d.node_id) }`
——**peer_loop 是按「已配对设备」起的**。所以：

> 🔴 **拓扑 = 配对图。星型是配出来的，不是写出来的。**

只要 A/B/C 各自只跟常驻机 H 配对、彼此不配对，A 的 `device_list` 里就只有 H
→ 只起 1 个 peer_loop。全网对数从 **N(N-1)/2 降到 N-1**：

| 台数 | 全配对（全网状） | **星型配对** |
|---:|---:|---:|
| 5 | 10 对 | **4 对** |
| 8 | 28 对 | **7 对** |

而且它顺带解决了 M6 设计稿 §2.2 记的那个硬限制——「两台都需在线才能直连同步」：
只要 H 常开，任何一台上线都能同步，**不需要两台叶子设备碰在一起**。
设计稿当时已经指出这条路：「得有一台常开机设备（旧笔记本 / NAS）当常驻 peer
——那本质是**中继/peer，不是你的中心服务器**」，**不破零第三方**。

⚠ **星型的代价（要告知用户，别默默换）**：
- **H 掉线 = 全停**。全网状是优雅降级（少一台只少一条路），星型是**单点**。
- H 必须真的常开，否则比全网状还差。

🔴 **所以产品侧缺的是引导，不是能力**：现在配对 UI 不会告诉用户
「设备多于 4 台时建议都配到一台常开机，而不是两两互配」。
没人说的话，用户默认就会配成全网状。

### ✅ 止血三条（都很小，不动拓扑）——**2026-09-07 已全部落地**，实现细节见 §四

> ⚠ 星型不能替代这三条：**我们保证不了用户一定配成星型**，
> 而且即使星型，H 那一台仍然同时面对 N-1 个对端（并发闸对它最刚需）。

1. ✅ **加全局并发闸**：`MAX_CONCURRENT_SESSIONS = 2`，限制本机同时进行的会话**总数**
   （拨出 + 接入合计）。直接压掉磁盘/内存尖峰，是最划算的一改。
2. ✅ **抖动按周期比例给**：`JITTER_SECS` 10 → **90**（心跳的 15%）。
3. ✅ **写入唤醒加合并窗口**：`coalesce_writes()`，3 秒安静 / 30 秒封顶。
   `MIN_SESSION_GAP_SECS=10` 挡的是**单对端**，挡不住广播。

---

## 三、调研：为什么不现在换技术路线

### iroh-docs（我们已经在用 iroh）

n0 的独立 crate，在维护（195 commits，文档更新到 2026-04）。
多写者 KV replica + **范围集合对账（RBSR，Meyer 2023）**，内容经 iroh-blobs 内容寻址。

直接解决 W1（blobs 天然搬附件）、W2（O(log n) 发现分叉并自愈）、W5、W6。

**但：**
- 它是 **per-key LWW**，**W4 一点不解决**
- 数据模型要从「SQLite 笔记行」映射成 KV entry

### 🔴 2026-09-07 重新调研：上面那条「删除语义零说明」要更正

> 上一轮只查了 README 与 docs.iroh.computer，**没人翻过源码/API 文档**。
> 翻了之后：**删除是有的**，但冲出一个更硬的限制。

`Replica::delete_prefix` 存在，API 文档原文：

> "Delete entries that match the given `author` and key `prefix`. This **inserts an empty entry**
> with the key set to `prefix`, effectively clearing all other entries whose key starts with
> or is equal to the given `prefix`."

| | 上一轮的认知 | 实际 |
|---|---|---|
| 有没有删除 | 「零说明」 | ✅ 有 `delete_prefix` |
| 怎么表示删除 | 未知 | **插入空条目**——就是墓碑，只是没叫这名字 |
| 存储回收 | 未知 | ❌ **仍未说明**（这条没变） |
| **跨作者删除** | 没查到 | 🔴 **新发现的硬伤**：`delete_prefix(author, prefix)` **绑定 author** |

🔴 **最后那条是关键**：多写者场景下 **A 删不掉 B 写的条目**。
而本项目天天发生这个——一篇笔记在台式机建、在笔记本上删。
现有实现靠 `note_tombstones` 表 + 三条物理删路径已经解决了；
**换过去这个能力会退化**。

### 另两条新成本

- **它还是 0.x，而 iroh 已 1.0**。最新 **0.101.0（2026-06-15，与 iroh 1.0 同日）**，
  依赖 `iroh ^1` —— **版本是对得上的**（本项目 iroh 1.1.0），但我们刚从 0.x 熬到 1.1，
  换过去等于退回快速迭代的 API
- **依赖面显著变大**：要新拖进 `iroh-blobs ^0.103` + `iroh-gossip ^0.101` + `redb ^4.1`。
  当前 iroh 净增体积已实测 3.94MB，这三个是新的一大块
- 项目规模：**73 star / 195 commit / 15 fork**（n0 官方但小众）

结论：**仍然不能换**，但阻塞点从「一片空白」收窄成**两个可问的具体问题**：
① 跨作者删除怎么办；② 空条目占的存储回不回收。
**可以去提 issue 了，不用再猜。**

查证出处：`docs.rs/crate/iroh-docs/latest`、
`docs.rs/iroh-docs/latest/iroh_docs/sync/struct.Replica.html`、`github.com/n0-computer/iroh-docs`。

### CRDT（Loro / Automerge / Yjs）

W4 彻底消失（字符级合并，没有「冲突副本」这个概念）。Obsidian LocalSync 就是 Yjs。

代价是**换掉存储模型**：正文现在是纯 markdown 存在 SQLite 里。
Loro 的编码比 Yjs/Automerge 小 2–5×，但业界实测元数据开销仍在 40–60%
（Automerge 约 4–6 字节/字符），生产系统里元数据超过正文 2–3× 是常态。

还有一条本项目特有的：知识库是**人 + AI 双重定位**，AI 写入会产生大量操作，
CRDT 历史增长会比纯人工编辑快得多。

结论：**不做。** 收益/代价比不划算。

---

## 四、取舍（已拍板 2026-09-07）

1. **W1 单张图片上限 = 10MB**。超过就跳过并在报告里说明（不静默）。
2. **W3 安全期 = 回收站保留期 + 30 天**。宁大勿小。
3. **W2 桶数 = 16**。

### ✅ 方向复核与拍板（2026-09-07）

起因：用户确认现在有 **>4 台**的需求，W6 阈值触发，顺带重查了一次「别人怎么做」。

**同类产品实际上是三种路子**：

| 产品 | 存储 | 同步方式 |
|---|---|---|
| **Obsidian** | vault = 文件夹里的普通文件 | **自己不做同步**，交给 iCloud/Syncthing/Dropbox；官方 Sync 是**服务端** E2EE |
| **Joplin** | **本地 SQLite（与本项目相同）** | 每条笔记写成纯文本文件，推到 Dropbox/OneDrive/WebDAV/文件系统；冲突进 `Conflicted` 副本 |
| **Syncthing** | 文件夹 | P2P 块级增量（本项目的参照物） |

🔴 **发现的一处评估不一致**（记下来，但不改变结论）：
M6 设计稿 §2.1 接受 iroh 的理由之一是「中继只读密文」，
却以「数据过别人服务器」拒了网盘（加密一栏写「取决于云」）。
但若**我们自己在写入前加密**（Joplin 就是这么做的），网盘同样只看到密文——
**同一个论证没有被一致地应用**。另：`sync/engine.rs` 的导出/导入本来就是对着**目录**做的，
iroh 只在 `transport` 那一层，**换传输不用重写同步**（attach/墓碑/冲突副本/游标全不相关）。

#### ✅ 拍板：**坚持零第三方，哑存储 transport 不做**

已知上述分析后仍选择只保留 P2P。本条**不需要再论证**：
不是因为哑存储不可行，而是**零第三方是产品选择**。
以后再提「不如走网盘」时，需要的是**新情况**（如零第三方在 N 台下真的支撑不住），
而不是重新论证「Joplin 都这么做」。

**直接后果**：>4 台只能在 P2P 内部解决 → 星型配对 + 止血三条，见 W6。

### ✅ 待拍板：已全部清空（2026-09-07）

4. ~~**W6 止血三条做不做**~~ → ✅ **已做完**（2026-09-07，见下一节）。
5. ~~**W5 是否重估**~~ → ✅ **重估完毕，仍然不动**（见 §W5）。
   并发闸把暂存目录峰值压回 2，而 2 正是当初「当前规模」那个前提的量级——
   不是结论巧合还成立，是**失效的前提被闸重新建立了**。
   重新打开的条件已写成可判定的，见那一节。

### ✅ ⑥ 星型配对的引导已落地（2026-09-07）

落在 `KbSyncPanel.tsx`：**本机对端数 ≥ 4**（= 至少 5 台，对应「>4 台」）时，
在设备列表下方显一段中性提示（同「外网」那段的样式，不报警），
按实际台数算出两两互配与星型各需多少对连接。

🔴 **阈值看的是本机对端数，不是总台数**——成本就是按对端算的：
每多一个直连对端，后端就多跑一条 `peer_loop`。

> ❗ **文案不能写成「你配错了」。** 面板只知道本机自己的对端数、
> 看不到全局拓扑，而**星型里的那台常开机同样有 N-1 个对端**——
> 它恰恰是配对了的那一台。光看对端数区分不了「全互配节点」与「星型中心」，
> 所以最后一句是「如果这台就是你的常开机，那现在这样就是对的」。

确实是零后端改动：`service.rs:814` 是 `for d in &known { start_peer(...) }`，
**拓扑就是配对图**，星型是「配出来」而不是「写出来」的。
缺的一直只是没人告诉用户还有这个选项。

### ✅ W6 止血三条已落地（2026-09-07）

| # | 改什么 | 落到哪里 |
|---|---|---|
| 1 | **全局并发闸** `MAX_CONCURRENT_SESSIONS = 2` | `coordinate.rs`：`Coordinator` 多一把 `Semaphore`，`Hold` 多持一个 `OwnedSemaphorePermit`（析构即归还） |
| 2 | **抖动按比例** `JITTER_SECS` 10 → 90 | 同上；心跳从 ±1.7% 撑到 ±15%（[510, 690]） |
| 3 | **写入合并窗口** 3 秒安静 / 30 秒封顶 | `service.rs`：新增 `coalesce_writes()`，接在 `idle_wait` 的脏判定之后 |

**实现时碰到的三个非显然点**（代码里都写了理由）：

- 🔴 **`try_hold` 必须区分「闸满」与「对端在忙」**（`HoldErr` 两个变体）。
  合成一个 `None` 的话，`admit` 会把闸满当成撞车去走 RFC §6.8 让位——
  而让位等的是那把**槽**的释放通知，槽根本没被这个对端占着，只会白等满 `YIELD_WAIT`。
- 🔴 **闸满的拒绝理由必须带「稍后重试」**。`service::is_busy_reject` 靠字样认，
  认不出就会被当成**故障**走退避阶梯：一台正常设备被退到 300 秒一拨，
  而本机可能下一秒就空出来了。已用一条跨文件断言钉住。
- 🔴 **合并窗口不能听 `ctx.wake`**。看着很像该听（「立即同步」应该不等），
  但 `wake` 同时是**组播公告的出口**——局域网里每台已配对设备每 15 秒打一次，
  听了窗口在局域网上几乎立刻被短路，整个合并形同虚设。
  代价已知：连续打字时点「立即同步」最多等 30 秒。

**验证**：`cargo test --lib sync::` 全绿（151 项，其中 11 项新增/改写），无新增编译警告。
新增测试里有两条是盯「**改了不会报错**」的：
`test_抖动幅度要按心跳比例给`（心跳从 30 改到 600 时抖动没跟着改，就是这次的成因）与
`test_闸满时拒的是可重试的在忙而不是故障`（跨文件字样一致性）。

❗ **三条都是单机可测的行为，但效果要 >4 台才看得出来**。进 §五的待实测清单。

🔴 **不建议的：现在换 iroh-docs**。原定「>4 台就直接评估 iroh-docs」这条路，
被 2026-09-07 的重新调研堵住了（跨作者删除 + 存储回收，见 §三）。
短期是**星型配对 + 在现有全网状上加闸**，不是换协议。

## 四半、全模块审查（2026-09-07）

通读了 `sync/` 全部 12 个模块 + `data_store` 相关部分 + 命令层。

### ✅ 已修

**① 增量清单路径穿越（高危）**

`transport.rs` 的模块注释写着「**不用 tar/zip**，因为解压器是路径穿越的经典入口；这里的名字自己校验（`safe_rel`），**校验规则只有一处**」。
——**那个前提不成立**。`engine::apply_delta` 从 `.pp-sync-manifest` 读第三列直接 `dir.join(rel)`，
而那条路径下面就是 `std::fs::remove_file`。文件**名**过了校验，文件**内容**没人管。

利用链（已写成回归测试，**并在旧代码上实测为红**）：
恶意对端写一行 `<它同步过的真实 note_id>\t0\t<inbox 外面的路径>` →
`incoming = 0` 使 `both_changed` 为假（不生冲突副本、不留痕迹）→ `local >= 0` 恒真 → 删除。
旧代码的测试输出 `skipped_older: 1`，正是执行 `remove_file` 那条分支。

修法：`safe_rel` 改成 `pub(super)`，清单与附件清单两处都过它。
发现穿越写法**直接报错中断会话**（正常对端的 `rel` 是 `write_delta` 生成的，出现穿越只有恶意或对端坏了两种可能）。

> 🔴 教训不是「`safe_rel` 写得不好」——它写得很好。是**「只有一处」这个断言随着新模块悄悄失效了**，
> 而注释里那句话反而让人放心。现在改成了「边界只有一道，但**入口不止一个**」。

**② 组播只走默认网卡（高危）——`lan_sync` 修过的同一个 bug**

`lan_sync.rs:85` 有一段 2026-09-06 写下的 🔴：`Ipv4Addr::UNSPECIFIED` 在组播语境下
**不是「所有网卡」**，是「让系统按路由表挑一块」；装了虚拟机/WSL/Docker/VPN 的机器上经常挑中虚拟网卡。
它为此加了 `enumerate_ifaces()` + 逐块 `set_multicast_if_v4` / `join_multicast_v4`。

**而 `sync/presence.rs` 用的正是被否定的那个写法**。本模块里症状更隐蔽：不是「同步不了」，
而是 `service::target()` 拿不到局域网地址 → 退到只给 node_id 的兜底分支 → **走 n0 公共中继**，
而 `transport_of()` 的判据就是 `addrs_of` 是否为空，所以徽章还会显示「外网」。

❗ `BACKOFF_STEPS_SECS` 的注释里那句「2026-09-06 实测：每轮都在跟 `euc1-1.relay.n0.iroh.link` 握手」，
很可能就是这个 bug 的现场，当时归因到了别处。

修法：把 `lan_sync::multicast_ifaces()` / `send_via_iface()` 提成 `pub(crate)`（后者组播地址与端口参数化），
`presence` 直接复用——**连那 30 秒网卡缓存一起复用**，`netdev::get_interfaces()` 不便宜。
两侧都加了「一块都没成功」的 `error!`（规则 #15.3）。

> ❗ 与 `lan_sync` 有一处**故意不同**：那边 join 一块不成就放弃线程，这边不。
> 本模块的宣告**单向也有用**：只要对端听得见我们，它就会拨过来，而同步会话本来就是双向的。

**③ `mod.rs` 的模块文档过期**：开头写着「还没有界面、不接任何 Tauri 命令」，
而 `commands/kb_sync.rs` 有 11 个命令。它是整个模块的门面。已改成指向命令层与界面位置。

### ✅ ④ 冲突副本去重（拍板后修于 2026-09-07）

改之前一次冲突在**每台机器上生两份**：赢家存「对端」那份、输家存「本机」那份。
而两者是**同一个版本**：`losing_ms` 相同、标题逐字相同，区别只在「来自对端/本机」一词——
**而那个词同步过去之后就没意义了**。两份携带的信息量完全相同，不是冗余备份，是纯重复。

对称性是可证的：两端的 `since` 来自同两个 hello，`local`/`incoming` 是同两个数字对调，
所以 `both_changed` **必然同时成立**——赢家不存，输家那边一定会存。

修法：严格赢（`local > incoming`）不存副本，**但保留 `rep.conflicts += 1`**。

> 🔴 两个坐标必须同时成立，少一个就是新 bug：
> - **计数要留**——不留的话赢家那台机器界面显「冲突 0 处」，用户压根不知道出过冲突。
> - **平手（`local == incoming`）仍然要存**——那时两边都走 `local >= incoming`、
>   都没有输家，不存就是两台机器各留各的、**永久静默分叉**。
>   最初的改法漏了这一条，是重读 `local >= incoming` 时发现的。

顺带修了副本正文的措辞：它原本写「来自**本机**那一份」，
而冲突副本本身就是一篇普通笔记、会跟着同步到对面去——到了那台机器上「本机」指的就是别人。
现在用时间戳标识那一版，在哪台机器上读都成立。

**代价（已知）**：输的那份内容有一小段时间只存在于输家那台机器上（本轮 apply 之后 → 下一轮同步之前）。

### ✅ ⑤ 会话超时：停滞超时，**不是**总超时（拍板后修于 2026-09-07）

🔴 **总超时是个陷阱，没采用。** 理由在代码里：`session::run` 只在**整轮成功之后**才推游标，
所以中断 = 没有任何部分进度。一旦总超时短于实际需要，那次同步就**永远做不完**——
不是变慢，是永久失败且每次重试烧同样的流量。而首次同步带图知识库走中继时几十分钟是可能的：
**拍不出一个安全的总时长。**

改成按进度算的 `STALL_TIMEOUT = 30s`：大文件分块（64 KiB）收发、每块各自计时，
换算下来最低吞吐要求 ≈ 17 kbps。**对「慢但在动」零误伤**，而慢速攻击 30 秒内被砍。

三处都包了（漏一处就还是能挂死）：
1. `read_frame` / `read_dir` 的头部读
2. `read_dir` 的内容读——**这处改成了分块**，一次 `read_exact` 整个文件再包超时等于又变回总超时
3. **`wr()` 的写**——对端不读的话流控窗口填满后 `write_all` 永远阻塞，只包读会漏掉一半

另外找到了一处审查时漏掉的无界等待：`exchange` 的 `w.conn.closed().await`（接受侧发完等排空）。
给了单独的 `CLOSE_WAIT = 60s`（`write_dir` 能返回就说明数据已被流控接收，剩下大约只有一个窗口），
且**超时就报错**：当成功的话本机推游标而对端没收到就不推，本机那批数据**再也不会被重发**。

#### 实测：没有它真的会挂死

回退修复后跑新测试，拿到的不是 FAILED 而是：

```
test test_对端发到一半不发了要中断而不是挂死 has been running for over 60 seconds
```

❗ 这同时验证了一条假设：**QUIC 自己的 idle timeout 并没有兜住它**。
所以代码注释里那句「不需要去依赖 iroh presets 的 idle timeout」是有实测支撑的，不是推测。

### ✅ ⑥ 文件夹移动现在能同步了（2026-09-07）

原本归类成「既有有意取舍」（`note.rs:197` 明列四处故意不刷 `updated_ms`），
但**那个阻塞理由已经失效了**：它说不敢刷是因为「会让最近修改列表乱跳」，
而那指向的是 `updated_at`（TEXT、界面排序键、有索引）——
**M6-P2 已经把同步的行版本号迁移成了另一个列 `updated_ms`（INTEGER）**。
两者独立，可以只刷后者。那句「等 M6 主体决定是否拆出独立行版本号」描述的决定，P2 已经做完了。

🔴 **但光刷戳没用。它同时被三处拦着，而且每一处都已经实测验证过是必需的：**

| | 拦在哪 | 单独回退它的实测结果 |
|---|---|---|
| ① | `note_set_folder` 不刷 `updated_ms` → 压根不进增量 | — |
| ② | 回声拦截只比标题/正文/标签 | `identical: 1` ——纯移动被当成「一模一样」吃掉，文件从暂存删掉、根本不导入 |
| ③ | `note_import_dir` 只在**新建**分支设文件夹 | 笔记传过去了，但**留在旧文件夹**（left: 工作 / right: 归档） |

❗ 第②处只加在**回声拦截**那一处，**没动 `same_version` 本体**。
它还被 ③ 之后的「真的落地了吗」校验用着，那里比不上会**按住游标**——
把文件夹掺进去就是「万一哪次文件夹没落地就游标永远不推、无限重试」。
（文件夹真落不下去时 `note_set_folder` 会报错，那一篇计入 `import_failed`，游标照样被按住。）

文件夹按**路径**比而不是 `folder_id`：id 是每台机器各自生成的，跨机没有可比性。

⚠ **第③处一并改变了普通 vault 导入的行为**（已拍板）：一篇已存在的笔记在 vault 里
被挪到另一个目录后导入，现在会跟着挪。与本模块既定的「**文件就是权威**」一致。

### ✅ ⑦ `%TEMP%` 明文笔记回收（2026-09-07）

`session::run` 在每条退出路径上都删了 `inbox`，但进程崩溃（或被杀）时会残留，
而之前**没有任何地方清理它们**——一台用了一年的机器上可以积很多份笔记明文。

同步服务启动时扫一遍 `%TEMP%` 顶层（不递归），删掉超过 24 小时的 `pp_session_*`。

> ❗ 两条安全线，都有测试盯着：
> - **只删带自己前缀的**。`%TEMP%` 是共用的，认错前缀就是删别人的文件。
>   `scratch()` 的前缀也改成读同一个常量，两处走形就是「永远清不掉」。
> - **24 小时是故意宽的**。误删一个正在跑的会话目录 = 那次同步直接崩；
>   而有了停滞超时之后，单次会话再慢也到不了这个量级。
>   年龄读不到时**不删**（宁可留着）。

### 🔴 一条核实过的不变量：`updated_ms` 是同步可见性的**唯一**判据

增量按它筛（`note_changed_since`），**分桶摘要也只吸收 `(id, updated_ms)`**
（`note.rs:753` 的 `sync_bucket_digests`，不碰内容也不碰摘要）。

所以那几个「不刷 `updated_ms`」的字段是**一致地隐形**，不是半可见。
这条很重要：否则会出现「桶一直报分叉、而增量又搜不到东西去修」的**永久不可修复分叉**。
（我一度怀疑 `summary` 就是这种情形，查了 `sync_bucket_digests` 才确认不是。）

✅ 本轮给文件夹刷 `updated_ms` 也没破这条：它同时对增量和摘要变可见。

### ✅ ⑧ `adopt_assets` 只落盘形状对的名字（2026-09-07）

上一轮结论是「不构成实际危害」——仍然成立，但**「现在恰好没危害」不是个好的停留状态**，
尤其是刚因为「下游恰好只是 `is_file()`」放过一个路径穿越（见 ①）。

换了一个比「加校验」更强的论证：附件名形状是 `{32位hex}.{1~5位小写}`，
由 `AssetRef::file_name()` 生成，而 `to_local()` **只会产出这一种引用**。
所以不符合形状的文件**不可能被任何笔记引用到**——落盘它是可证明的纯浪费。
不是「加一道安全门」，是「把不可能有用的东西挡在外面」，零取舍。

❗ 写测试时踩到一个坑：**Windows 文件名大小写不敏感**，
大小写变体拿同一个 hash 的话 `xxx.PNG` 与 `xxx.png` 会落成同一个文件，
把那个真附件覆成「垃圾」——测试会**假绿**。已改成用互不相同的 hash。

### 审出、仍属**既有有意取舍**

#### `summary` 不刷戳 —— ✅ **已定型，不用再看**

⚠ 上一轮写的「不同步是对的」**不准确**。查了之后：`summary` 在 frontmatter 里
（`note_md.rs:107`），`note_import_dir` 也会写回去。真实行为是：

> **笔记因为别的原因同步时，摘要顺路过去；摘要自己不制造流量。**

这恰好是最优解：AI 批量生成摘要不会引发同步风暴，而摘要也不会白丢。
**从待办列表里划掉。**

#### 置顶（`pinned`）不同步 —— **不做，但触发条件已写死**

🔴 先说一件比它本身重要的事：**这三条的问题不在决定本身，在它们是怎么记的。**

`folder_id` 已经证明过一次。它当时记的是「等 M6 主体决定是否拆出独立的行版本号」，
而它等的那件事——`updated_ms` 拆成独立列——**M6-P2 早就做完了**。
条件满足了几个月没人回来看一眼，因为那句话没有可判定的触发条件，
只有一个「等某个人某天决定」。

> ✅ **以后这类搁置一律写成「满足 Y 就做」，不写「等 X 决定」。**

置顶的具体安排：

| | |
|---|---|
| **现在** | **不做**。收益（置顶跟着走）远小于代价，而结构还没定 |
| **触发条件** | 出现**第二个**「只在库里、不在 markdown 里」的字段时 |
| **那时怎么做** | ⚠ **不要往 frontmatter 塞第二个**。清单已经带着每篇元数据（`id \t ms \t rel`），加一份「同步专用字段」旁挂文件，N 个字段一次性解决，且完全不动 vault 格式与 Obsidian 互操作性 |

往 frontmatter 塞了第二个，格式就回不来了——那才是不可逆的那一步。

❗ 另外修正一个我自己的误判：它**不是「3 行」**。真要加 frontmatter 字段，
要做的事与文件夹一模一样：**同一形状的三处改动**
（切换时刷戳 + 回声拦截要比它 + 导入要写回它）。
每多一个这类字段就要重跳一次——这正是旁挂方案要解决的。

### 审了但没找到问题的

`presence` 的收包校验（长度帽、版本、node_id 长度、**验签在动表之前**、时钟窗口、
`ts` 严格递增防重放、地址数封顶 + LRU 淘汰）、`transport` 的帧与长度夹取（**分配之前**夹单文件）、
`session` 的 HLC 吸收顺序与游标夹取（`unsettled_min_ms - 1`）、`hlc` 的 `fetch_update` 严格递增。
这几处都是对的，而且难写对。

---

## 五、尚未双机实测

上一批（commit `a7d0eff`）里这些只过了单机测试：
脏了才拨的实际会话频率、写入唤醒的延迟、公告跃变叫醒。

### W6 止血三条（2026-09-07）

三条的**行为**已经被单测钉住，但**效果要 >4 台才看得出来**。待实测：

| 要验的 | 怎么看 |
|---|---|
| 全局闸真的把尖峰压到 2 | 开 5+ 台，改一篇笔记，数 `%TEMP%\pp_session_*` 同时存在的个数 |
| 排队的对端没被当成故障 | 日志里应该是 `debug` 的「这次没轮到我们」，**不应该**出现退避阶梯的 `warn` |
| 心跳不再聚群 | 几台同时开机后放着不动，看各自的心跳会话时间是否散开（应在 ±90 秒内散开） |
| 合并窗口没把延迟拖得难受 | 改一字 → 对端看到的延迟应在 ~3 秒 + 会话时间以内 |

❗ 第二行最值得盯：它正是「拒绝理由字样对不上」时唯一看得见的症状。
