{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "rc-workbench-close",
  "description": "远程电脑工作台专用的窗口权限。本窗口是自绘标题栏（decorations(false)），窗口按钮由前端 RcWindowControls 调用；最小化 / 关闭 / 拖拽已在 default.json（该文件的 windows 列表本就含 rc-workbench），这里只补它独有的三项：destroy（关闭守卫收尾）+ 最大化切换与状态查询。",
  "windows": ["rc-workbench"],
  "permissions": [
    "core:window:allow-destroy",
    "core:window:allow-toggle-maximize",
    "core:window:allow-is-maximized"
  ]
}
