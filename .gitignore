# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets
.env.secrets

# Runtime config backups (rotated by the app, not source)
src-tauri/config_backups/

# 早期会话的临时产物（图片 base64 中转、暂缓的测试草稿）
melody_b64.txt
*.test.ts.pending

# codegraph 索引产物（本地生成，勿入库）
.codegraph/

# 兑换码生成器的 Python 字节码（.pyc 里会带上运行时用过的字符串）
tools/__pycache__/
# 项目记忆（工作日志，本地开发档案，不入库）
.codebuddy/
src-tauri/.libclang/
.workbuddy/
